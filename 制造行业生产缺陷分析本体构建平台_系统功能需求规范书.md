# 制造行业生产缺陷智能分析平台

## 系统功能需求规范书（SRS）

| 项目 | 内容 |
|------|------|
| 文档名称 | 制造行业生产缺陷智能分析平台 — 系统功能需求规范书 |
| 文档版本 | V1.0 |
| 阶段 | 第一阶段 — 企业生产过程本体对象构建 |
| 编写日期 | 2026-07-13 |
| 文档状态 | 评审稿 |

---

## 目录

- [1. 项目概述](#1-项目概述)
  - [1.1 项目背景](#11-项目背景)
  - [1.2 项目目标](#12-项目目标)
  - [1.3 建设范围](#13-建设范围)
  - [1.4 术语与缩略语](#14-术语与缩略语)
- [2. 系统总体架构](#2-系统总体架构)
  - [2.1 架构总览](#21-架构总览)
  - [2.2 技术选型](#22-技术选型)
  - [2.3 系统分层设计](#23-系统分层设计)
- [3. 功能模块详述](#3-功能模块详述)
  - [3.1 模块一：源数据浏览管理](#31-模块一源数据浏览管理)
  - [3.2 模块二：本体对象构建](#32-模块二本体对象构建)
  - [3.3 模块三：本体对象数据映射](#33-模块三本体对象数据映射)
  - [3.4 模块四：本体对象 DDL 生成与应用](#34-模块四本体对象-ddl-生成与应用)
  - [3.5 模块五：本体对象浏览管理](#35-模块五本体对象浏览管理)
  - [3.6 模块六：系统管理](#36-模块六系统管理)
- [4. Agent 智能体与 Skill 设计](#4-agent-智能体与-skill-设计)
- [5. 非功能性需求](#5-非功能性需求)
- [6. 数据库设计要求](#6-数据库设计要求)
- [7. 接口规范](#7-接口规范)
- [8. 第一阶段交付物清单](#8-第一阶段交付物清单)

---

## 1. 项目概述

### 1.1 项目背景

在制造行业生产过程中，产品缺陷的识别、根因分析与影响评估是保障产品质量和生产效率的关键环节。传统缺陷分析依赖人工经验，存在分析效率低、根因追溯困难、缺陷影响范围评估不全面等问题。

随着大语言模型（LLM）和知识图谱技术的成熟，基于本体论（Ontology）方法构建企业生产过程知识体系，结合 Agent 智能体与 Skill 技能编排，可以实现缺陷分析过程的智能化、自动化和可追溯化。

本平台以 Oracle 26ai 数据库为底座，利用其原生图谱（Oracle Graph）能力与向量检索能力，构建生产过程本体知识图谱，并通过 Agent + Skill 架构实现缺陷智能分析。

### 1.2 项目目标

| 目标层级 | 目标描述 |
|----------|----------|
| **总体目标** | 构建面向制造行业的生产缺陷智能分析平台，实现缺陷分析、根因分析、缺陷影响分析三大核心能力 |
| **第一阶段目标** | 完成企业生产过程本体对象的构建能力，包括源数据管理、本体定义、数据映射、DDL 生成与应用、本体浏览管理及系统管理 |
| **技术目标** | 基于 Oracle 26ai 数据库，采用本体论方法，通过 Vue + Python 技术栈，集成 LLM 大模型，实现本体构建全流程的智能化辅助 |

### 1.3 建设范围

#### 1.3.1 第一阶段范围（本期）

本阶段聚焦于**本体对象构建全流程**，包含以下功能模块：

```
┌─────────────────────────────────────────────────────────────┐
│                  制造行业生产缺陷智能分析平台                     │
│                      （第一阶段建设范围）                        │
├──────────┬──────────┬──────────┬──────────┬────────┬────────┤
│  源数据   │  本体对象  │  本体对象  │  本体对象  │ 本体对象 │  系统   │
│  浏览管理  │   构建    │  数据映射  │ DDL生成   │ 浏览管理 │  管理   │
│          │          │          │  与应用   │         │        │
└──────────┴──────────┴──────────┴──────────┴────────┴────────┘
```

#### 1.3.2 后续阶段范围（规划中）

- 缺陷分析 Agent 智能体实现
- 缺陷根因分析 Skill 开发
- 缺陷影响分析 Skill 开发
- 数据分析流程执行引擎
- 分析后 Action 自动化执行

### 1.4 术语与缩略语

| 术语 | 全称 | 说明 |
|------|------|------|
| 本体 / Ontology | Ontology | 对共享概念化的明确、形式化描述，包含概念（实体）、属性、关系及实例 |
| 业务分析域 | Business Analysis Domain | 按照业务领域划分的本体管理单元，如"冲压缺陷域"、"焊接缺陷域"等 |
| 本体对象 | Ontology Object | 本体中的实体对象，对应业务概念，如"产品"、"工序"、"缺陷"等 |
| 本体属性 | Ontology Property | 本体对象的属性字段，描述实体的特征 |
| 本体关系 | Ontology Relation | 两个本体对象之间的有向关系，如"属于"、"导致"、"影响"等 |
| Management View | Management View | 以视图（View）方式构建本体实体实例 |
| Management Table | Management Table | 以表（Table）方式构建本体实体实例 |
| DDL | Data Definition Language | 数据定义语言，用于创建/修改数据库对象 |
| Oracle Graph | Oracle Property Graph | Oracle 数据库原生属性图功能 |
| Agent | AI Agent | 基于大模型的智能体，具备感知、推理、决策与执行能力 |
| Skill | AI Skill | Agent 可调用的技能模块，封装特定领域分析能力 |
| LLM | Large Language Model | 大语言模型 |
| SRS | Software Requirements Specification | 软件需求规范书 |

---

## 2. 系统总体架构

### 2.1 架构总览

系统采用前后端分离的分层架构，整体分为**展示层、应用层、服务层、数据层**四个层次：

```
┌──────────────────────────────────────────────────────────────────────┐
│                          展 示 层 (Vue Frontend)                       │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────┐ ┌────┐ │
│  │源数据浏览│ │本体构建   │ │数据映射   │ │DDL管理    │ │本体浏览│ │系统│ │
│  │管理界面  │ │图形编辑器 │ │映射界面   │ │编辑界面   │ │图谱界面│ │管理│ │
│  └─────────┘ └──────────┘ └──────────┘ └──────────┘ └──────┘ └────┘ │
├──────────────────────────────────────────────────────────────────────┤
│                          应 用 层 (API Gateway)                       │
│         RESTful API  │  WebSocket(图形实时交互)  │  文件上传/下载       │
├──────────────────────────────────────────────────────────────────────┤
│                       服 务 层 (Python Backend)                       │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────┐ ┌────┐ │
│  │源数据    │ │本体构建   │ │数据映射   │ │DDL生成    │ │本体   │ │用户│ │
│  │服务      │ │服务      │ │服务      │ │服务      │ │浏览   │ │管理│ │
│  │          │ │          │ │          │ │          │ │服务   │ │服务│ │
│  └─────────┘ └──────────┘ └──────────┘ └──────────┘ └──────┘ └────┘ │
│  ┌───────────────────┐  ┌───────────────────┐  ┌──────────────────┐ │
│  │  LLM 调用服务      │  │  Agent 编排引擎    │  │  Skill 管理服务   │ │
│  │  (OpenAI 兼容接口)  │  │  (规划中)          │  │  (规划中)         │ │
│  └───────────────────┘  └───────────────────┘  └──────────────────┘ │
├──────────────────────────────────────────────────────────────────────┤
│                       数 据 层 (Oracle 26ai)                          │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐  ┌───────────────────┐ │
│  │ 源数据表   │  │ 本体元数据 │  │ 本体实例表 │  │ Oracle Graph 图谱  │ │
│  │(客户业务表)│  │ (平台管理) │  │(View/Table)│  │  (属性图)          │ │
│  └───────────┘  └───────────┘  └───────────┘  └───────────────────┘ │
└──────────────────────────────────────────────────────────────────────┘
```

### 2.2 技术选型

| 层次 | 技术选型 | 说明 |
|------|----------|------|
| **前端框架** | Vue 3 + TypeScript | 采用 Composition API，组件化开发 |
| **前端 UI 库** | Element Plus / Ant Design Vue | 提供企业级 UI 组件 |
| **图形编辑器** | AntV X6 / vue-flow | 用于本体关系图编辑、分析流程图编辑 |
| **图谱可视化** | AntV G6 / Cytoscape.js | 用于本体图谱浏览展示 |
| **后端框架** | Python 3.11+ / FastAPI | 高性能异步框架，自动生成 API 文档 |
| **ORM** | SQLAlchemy 2.0 | 支持 Oracle 数据库操作 |
| **Oracle 驱动** | oracledb (python-oracledb) | Oracle 官方 Python 驱动 |
| **LLM 集成** | OpenAI 兼容 SDK (openai / litellm) | 支持 OpenAI 兼容接口的大模型调用 |
| **数据库** | Oracle 26ai | 利用其 Graph、Vector、JSON 等多模态能力 |
| **任务队列** | Celery + Redis (可选) | 异步任务处理（DDL 执行、大模型调用等） |
| **认证** | JWT (JSON Web Token) | 前后端分离认证方案 |

### 2.3 系统分层设计

#### 2.3.1 展示层

- 采用 Vue 3 SPA（单页应用）架构
- 通过 RESTful API 与后端交互
- 图形化操作通过 WebSocket / SSE 实现实时反馈
- 响应式布局，适配主流桌面浏览器（Chrome、Edge、Firefox）

#### 2.3.2 应用层

- 基于 FastAPI 提供 RESTful API
- 统一鉴权与权限控制
- 请求参数校验与统一异常处理
- API 文档自动生成（OpenAPI / Swagger）

#### 2.3.3 服务层

- 按业务域划分服务模块
- LLM 调用服务封装大模型交互逻辑
- 数据库操作通过 SQLAlchemy 统一管理
- 预留 Agent 编排引擎与 Skill 管理接口

#### 2.3.4 数据层

- **源数据表**：客户提供的 Oracle 业务表，存储生产过程原始数据
- **本体元数据表**：存储本体对象定义、属性定义、关系定义、映射配置等平台管理数据
- **本体实例表/视图**：根据 DDL 生成的实际物理对象
- **Oracle Graph 图谱**：基于本体关系构建的属性图

---

## 3. 功能模块详述

### 3.1 模块一：源数据浏览管理

#### 3.1.1 功能概述

为用户提供对 Oracle 数据库中源数据表的可视化浏览和管理能力，支持查看表结构、字段信息、Comments 信息，并允许在线修改 Comments 后持久化到数据库。

#### 3.1.2 功能清单

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|----------|----------|--------|
| F1-01 | 源表列表浏览 | 以树形/列表方式展示数据库 Schema 下的所有源表，支持搜索、筛选 | P0 |
| F1-02 | 表结构查看 | 查看指定表的字段列表，包含字段名、数据类型、是否可空、默认值等信息 | P0 |
| F1-03 | 表 Comments 查看 | 查看表的 Comments 描述信息 | P0 |
| F1-04 | 字段 Comments 查看 | 查看每个字段的 Comments 描述信息 | P0 |
| F1-05 | 表 Comments 修改 | 在线编辑表的 Comments，提交后写入 Oracle 数据库（`COMMENT ON TABLE`） | P0 |
| F1-06 | 字段 Comments 修改 | 在线编辑字段的 Comments，提交后写入 Oracle 数据库（`COMMENT ON COLUMN`） | P0 |
| F1-07 | Comments 持久化 | 修改后的 Comments 立即持久化到数据库，下次读取时反映最新值 | P0 |
| F1-08 | 源表数据查询 | 支持对源表执行 SQL 查询，支持条件筛选、排序、分页 | P0 |
| F1-09 | 源表数据预览 | 以表格方式预览源表数据，支持分页加载 | P0 |
| F1-10 | 批量 Comments 导入 | （扩展）支持通过 Excel/CSV 批量导入 Comments | P1 |

#### 3.1.3 详细要求

**F1-01 源表列表浏览**

- 从 Oracle 数据库 `ALL_TABLES` / `USER_TABLES` 读取当前 Schema 下的表清单
- 支持按表名关键字搜索（模糊匹配）
- 支持按 Schema 切换浏览（如有多 Schema 权限）
- 表列表中显示：表名、表 Comments（如有）、记录数（可选）
- 默认按表名字母排序

**F1-02 表结构查看**

- 读取 `ALL_TAB_COLUMNS` 获取字段详细信息
- 字段信息包含：字段名、数据类型及长度、是否可空（NULLABLE）、默认值、字段位置
- 同时读取 `ALL_COL_COMMENTS` 获取字段 Comments
- 以表格方式展示，支持字段名搜索

**F1-05 / F1-06 Comments 修改**

- 用户在界面点击编辑按钮，弹出编辑框
- 修改后点击"保存"，系统执行以下 SQL：
  - 表 Comments：`COMMENT ON TABLE <schema>.<table_name> IS '<comment>'`
  - 字段 Comments：`COMMENT ON COLUMN <schema>.<table_name>.<column_name> IS '<comment>'`
- 保存成功后刷新界面显示
- 支持 Comments 内容为空时清除（`IS ''`）
- 修改操作需要记录操作日志（操作人、时间、修改前/后内容）

**F1-08 / F1-09 源表数据查询与预览**

- 数据预览：默认查询前 100 条记录，以表格方式展示
- 支持用户自定义查询条件（字段 = 值 / LIKE / 范围等）
- 支持选择展示字段
- 支持分页（每页 50/100/200 条可选）
- 查询 SQL 需做 SQL 注入防护
- 大字段（CLOB/BLOB）类型仅显示类型标识，不显示内容
- 数据只读，不支持修改源表数据

#### 3.1.4 界面要求

```
┌─────────────────────────────────────────────────────────────┐
│  源数据浏览管理                                               │
├──────────────┬──────────────────────────────────────────────┤
│ Schema 选择   │  ┌─ 表结构信息 ──────────────────────────────┐ │
│ ┌──────────┐ │  │ 表名: DEFECT_RECORDS      [编辑Comments]  │ │
│ │ 搜索表名  │ │  │ Comments: 缺陷记录表                       │ │
│ └──────────┘ │  ├──────────────────────────────────────────┤ │
│              │  │ 字段名      类型       可空  Comments  操作│ │
│ ▼ DEFECT_    │  │──────────────────────────────────────────│ │
│   RECORDS    │  │ DEFECT_ID   NUMBER(10) N   缺陷ID    [编辑]│ │
│ ▼ PRODUCT_   │  │ PRODUCT_ID  NUMBER(10) N   产品ID    [编辑]│ │
│   INFO       │  │ DEFECT_TYPE VARCHAR2(50)Y  缺陷类型  [编辑]│ │
│ ▼ PROCESS_   │  │ ...                                       │ │
│   LOG        │  └──────────────────────────────────────────┘ │
│ ▼ ...        │  ┌─ 数据预览 ────────────────────────────────┐ │
│              │  │ [查询条件] [执行查询]    每页[100▼]条       │ │
│              │  │──────────────────────────────────────────│ │
│              │  │ DEFECT_ID | PRODUCT_ID | DEFECT_TYPE | ... │ │
│              │  │ 100001    | 200001     | 划伤        | ... │ │
│              │  │ 100002    | 200002     | 凹陷        | ... │ │
│              │  │ < 1 2 3 4 5 >                            │ │
│              │  └──────────────────────────────────────────┘ │
└──────────────┴──────────────────────────────────────────────┘
```

---

### 3.2 模块二：本体对象构建

#### 3.2.1 功能概述

提供图形化本体编辑能力，允许业务人员通过可视化方式定义本体实体、属性和关系。同时支持定义数据分析流程图及流程中的本体使用方式和 Action 操作。

按照**业务分析域**进行组织管理，每个分析域下包含独立的本体定义和流程定义。

#### 3.2.2 功能清单

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|----------|----------|--------|
| F2-01 | 业务分析域管理 | 创建、编辑、删除业务分析域，作为本体构建的组织容器 | P0 |
| F2-02 | 本体实体创建 | 通过图形化方式添加本体实体节点，定义实体名称、描述 | P0 |
| F2-03 | 本体属性定义 | 为实体添加属性，定义属性名称、数据类型、描述 | P0 |
| F2-04 | 本体关系定义 | 通过拉有向箭头定义两个实体之间的关系 | P0 |
| F2-05 | 关系类型管理 | 支持多种关系类型（一对一、一对多、多对多、继承、关联等） | P0 |
| F2-06 | 关系属性定义 | 在关系上定义关系名称、关系描述 | P0 |
| F2-07 | 本体图谱可视化 | 以图形方式呈现完整的本体实体-属性-关系图 | P0 |
| F2-08 | 分析流程定义 | 通过画流程图方式定义数据分析流程 | P0 |
| F2-09 | 流程 JSON 持久化 | 画出的流程以 JSON 格式写入数据库存储 | P0 |
| F2-10 | 本体使用方式定义 | 定义分析流程中本体对象的使用方式和使用时间 | P1 |
| F2-11 | Action 操作定义 | 定义分析流程在分析完成后的 Action 操作 | P1 |
| F2-12 | 本体定义导入/导出 | （扩展）支持本体定义的 JSON 导入/导出 | P2 |

#### 3.2.3 详细要求

**F2-01 业务分析域管理**

- 业务分析域是本体管理的顶层组织单元
- 每个分析域包含：域名称、域描述、创建人、创建时间、状态
- 分析域之间相互独立，本体定义不共享
- 支持分析域的创建、编辑、删除（删除时需确认，并检查是否有关联本体）
- 示例分析域：`冲压缺陷分析域`、`焊接缺陷分析域`、`装配缺陷分析域`

**F2-02 本体实体创建**

- 在图形画布上通过"拖拽"或"点击添加"方式创建实体节点
- 实体节点信息包含：
  - 实体名称（唯一，同分析域内不可重复）
  - 实体显示名称（中文业务名称）
  - 实体描述
  - 实体图标/颜色（可选，用于图形展示区分）
- 实体创建后保存到本体元数据表

**F2-03 本体属性定义**

- 选中实体节点后，在属性面板中添加/编辑/删除属性
- 每个属性包含：
  - 属性名称（英文字段名，同实体内唯一）
  - 属性显示名称（中文）
  - 数据类型（VARCHAR2, NUMBER, DATE, CLOB, 等）
  - 是否主键
  - 是否可空
  - 属性描述
  - 属性来源标记（预留：待映射 / 已映射）

**F2-04 / F2-05 / F2-06 本体关系定义**

- 在两个实体节点之间通过鼠标拖拽拉有向箭头建立关系
- 关系信息包含：
  - 源实体（起点）
  - 目标实体（终点）
  - 关系名称（如：`属于`、`导致`、`影响`、`包含`、`产生于`）
  - 关系类型：
    - `ONE_TO_ONE` — 一对一
    - `ONE_TO_MANY` — 一对多
    - `MANY_TO_MANY` — 多对多
    - `INHERITANCE` — 继承（is-a）
    - `ASSOCIATION` — 关联
  - 关系描述
  - 关系属性（可选，关系本身可以携带属性，如"影响程度"）
- 箭头方向表示关系的有向性
- 支持在同一对实体间定义多种关系

**F2-07 本体图谱可视化**

- 以力导向图或层次布局图方式展示完整本体关系图
- 节点表示实体，边表示关系
- 节点上可显示实体名称和关键属性
- 边上显示关系名称
- 支持缩放、平移、全屏、导出图片
- 支持按实体类型/颜色筛选显示
- 支持点击节点/边查看详细信息

**F2-08 / F2-09 分析流程定义**

- 提供独立的流程图画布
- 流程图节点类型：
  - `开始节点`（Start）
  - `数据输入节点`（DataInput）— 引用本体对象作为数据源
  - `分析节点`（Analysis）— 执行特定分析任务
  - `决策节点`（Decision）— 条件分支
  - `Action 节点`（Action）— 执行后续操作
  - `结束节点`（End）
- 节点之间通过有向连线连接，表示执行顺序
- 流程图保存时，系统将画布内容序列化为 JSON 格式
- JSON 存储到数据库的流程定义表中
- JSON 结构示例：

```json
{
  "processId": "proc_001",
  "processName": "冲压缺陷根因分析流程",
  "domainId": "domain_stamping",
  "nodes": [
    {
      "id": "node_1",
      "type": "start",
      "position": {"x": 100, "y": 100},
      "label": "开始"
    },
    {
      "id": "node_2",
      "type": "dataInput",
      "position": {"x": 300, "y": 100},
      "label": "缺陷数据输入",
      "config": {
        "ontologyEntity": "DefectRecord",
        "usageMode": "query",
        "usageTime": "T-1 ~ T"
      }
    },
    {
      "id": "node_3",
      "type": "analysis",
      "position": {"x": 500, "y": 100},
      "label": "缺陷分类分析",
      "config": {
        "analysisType": "classification",
        "skillId": "skill_defect_classify"
      }
    },
    {
      "id": "node_4",
      "type": "action",
      "position": {"x": 700, "y": 100},
      "label": "生成分析报告",
      "config": {
        "actionType": "generateReport",
        "template": "defect_analysis_report"
      }
    },
    {
      "id": "node_5",
      "type": "end",
      "position": {"x": 900, "y": 100},
      "label": "结束"
    }
  ],
  "edges": [
    {"source": "node_1", "target": "node_2"},
    {"source": "node_2", "target": "node_3"},
    {"source": "node_3", "target": "node_4"},
    {"source": "node_4", "target": "node_5"}
  ],
  "version": "1.0",
  "createdAt": "2026-07-13T10:00:00Z",
  "createdBy": "admin"
}
```

**F2-10 本体使用方式定义**

- 在分析流程的数据输入节点中，定义本体对象的使用方式：
  - `query` — 查询模式（只读）
  - `aggregate` — 聚合分析模式
  - `compare` — 对比分析模式
  - `trace` — 追溯分析模式
- 定义使用时间范围：
  - 时间窗口（如 T-7 ~ T，表示最近7天）
  - 具体日期范围
  - 相对时间表达式

**F2-11 Action 操作定义**

- 在分析流程的 Action 节点中，定义分析完成后的操作：
  - `generateReport` — 生成分析报告
  - `sendNotification` — 发送通知
  - `triggerAlert` — 触发告警
  - `updateOntology` — 更新本体数据
  - `customAction` — 自定义操作（预留扩展）
- 每个 Action 可配置参数

#### 3.2.4 界面要求

```
┌──────────────────────────────────────────────────────────────────────┐
│  本体对象构建 — [冲压缺陷分析域 ▼]                                      │
├──────────────────────────────────────────────────────────────────────┤
│  [Tab: 本体关系图]  [Tab: 分析流程图]                                   │
├──────────────────────────────────────────────────────────────────────┤
│  ┌─ 本体关系图画布 ──────────────────────────────┐  ┌─ 属性面板 ────┐  │
│  │                                              │  │ 实体名称:     │  │
│  │    ┌──────────┐         ┌──────────┐         │  │ DefectRecord │  │
│  │    │ 缺陷记录  │──导致──▶│ 缺陷类型  │         │  │              │  │
│  │    │ Defect   │         │ Defect   │         │  │ 显示名称:     │  │
│  │    │ Record   │         │ Type     │         │  │ 缺陷记录      │  │
│  │    └────┬─────┘         └──────────┘         │  │              │  │
│  │         │                                     │  │ 描述:        │  │
│  │    产生于                                     │  │ 记录生产过程  │  │
│  │         ▼                                     │  │ 中的缺陷信息  │  │
│  │    ┌──────────┐         ┌──────────┐         │  │              │  │
│  │    │ 工序信息  │──包含──▶│ 产品信息  │         │  ├─ 属性列表 ──┤  │
│  │    │ Process  │         │ Product  │         │  │ [+] 添加属性  │  │
│  │    └──────────┘         └──────────┘         │  │──────────────│  │
│  │                                              │  │ defect_id  PK│  │
│  │  [+] 添加实体  [↗] 添加关系  [💾] 保存        │  │ product_id   │  │
│  └──────────────────────────────────────────────┘  │ defect_type  │  │
│                                                    │ ...          │  │
│                                                    └──────────────┘  │
└──────────────────────────────────────────────────────────────────────┘
```

---

### 3.3 模块三：本体对象数据映射

#### 3.3.1 功能概述

将本体对象定义与物理数据库表进行映射，完成实体对象的实例化构建过程。明确每个属性的来源表、来源字段、计算方法和计算公式。支持 LLM 辅助自动映射，人工审核确认。

按照**业务分析域**进行管理。

#### 3.3.2 功能清单

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|----------|----------|--------|
| F3-01 | 实体构建方式选择 | 为每个实体选择构建方式：Management View 或 Management Table | P0 |
| F3-02 | 属性手动映射 | 手动为每个本体属性选择源表和源字段 | P0 |
| F3-03 | 属性计算方法定义 | 定义属性的计算方法、计算公式描述 | P0 |
| F3-04 | LLM 辅助自动映射 | 利用大模型根据本体定义自动推荐源表/字段映射 | P0 |
| F3-05 | 映射结果审核 | 用户对 LLM 推荐的映射结果进行审核、修改、确认 | P0 |
| F3-06 | 映射后本体图查看 | 查看完成映射后的本体图，展示属性映射和构造方法 | P0 |
| F3-07 | 关系映射定义 | 定义本体关系对应的物理表关联条件（JOIN 条件） | P0 |
| F3-08 | 映射状态管理 | 跟踪每个实体/属性的映射状态（未映射/映射中/已映射/已确认） | P1 |
| F3-09 | 映射版本管理 | （扩展）支持映射配置的版本管理与历史对比 | P2 |

#### 3.3.3 详细要求

**F3-01 实体构建方式选择**

- 每个本体实体可选择两种构建方式之一：

| 构建方式 | 说明 | 适用场景 |
|----------|------|----------|
| **Management View** | 通过创建数据库视图（VIEW）方式构建实体实例 | 实体数据完全来源于已有源表的查询聚合，不需要独立存储 |
| **Management Table** | 通过创建物理表（TABLE）方式构建实体实例 | 实体需要独立存储数据，或需要通过 ETL 过程填充数据 |

- 构建方式在映射阶段确定，影响后续 DDL 生成
- 支持在映射阶段修改构建方式

**F3-02 属性手动映射**

- 为每个本体属性指定数据来源：
  - **直接映射**：选择源表 + 源字段，属性值直接取自该字段
  - **计算映射**：通过计算公式从多个源字段计算得出
  - **常量映射**：固定值
  - **LLM 推导映射**：由 LLM 分析推导的映射关系（需人工确认）
- 映射信息包含：
  - 源表名
  - 源字段名
  - 映射类型（direct / computed / constant / llm_derived）
  - 计算公式（当映射类型为 computed 时）
  - 数据类型转换规则（如需要）

**F3-03 属性计算方法定义**

- 对于计算映射类型的属性，需定义计算方法
- 计算方法信息包含：
  - 计算方法名称（如 `SUM`、`AVG`、`COUNT`、`CASE_WHEN`、`自定义公式`）
  - 计算公式描述（自然语言描述，供业务理解）
  - 计算公式表达式（SQL 表达式，用于 DDL 生成）
  - 涉及的源表和字段列表
- 示例：
  - 属性 `defect_rate`（缺陷率）
  - 计算方法：`比率计算`
  - 公式描述：`缺陷数量 / 生产总数量 * 100`
  - 公式表达式：`CAST(defect_count AS FLOAT) / NULLIF(total_count, 0) * 100`

**F3-04 LLM 辅助自动映射**

- 系统调用 LLM 大模型，输入以下信息：
  - 本体实体定义（实体名称、描述、属性列表及描述）
  - 数据库源表的元数据信息（表名、表 Comments、字段名、字段类型、字段 Comments）
- LLM 分析本体属性与源表字段的语义匹配关系，输出映射建议：
  - 每个属性推荐的源表和源字段
  - 匹配置信度（高/中/低）
  - 匹配理由说明
- 支持批量映射和单实体映射两种模式
- LLM 调用过程需要：
  - 构造合适的 Prompt（系统提示词 + 上下文信息）
  - 处理大表的字段截断（当源表字段过多时，分批处理）
  - 解析 LLM 返回结果，结构化存储

**F3-05 映射结果审核**

- LLM 返回映射建议后，在界面上以对比表格方式展示：
  - 本体属性 | 推荐源表 | 推荐源字段 | 置信度 | 匹配理由 | 操作（接受/修改/拒绝）
- 用户逐条或批量审核：
  - **接受**：确认该映射
  - **修改**：手动调整源表/字段/计算方法
  - **拒绝**：标记为待映射，不采用 LLM 建议
- 审核完成后，映射状态更新为"已确认"

**F3-06 映射后本体图查看**

- 在本体图上增加映射信息展示：
  - 节点上标注构建方式（View/Table 图标）
  - 节点属性展开后显示映射的源表.源字段
  - 计算属性显示计算公式描述
- 支持筛选：只看未映射属性、只看已映射属性
- 映射完整度统计：已映射属性数 / 总属性数

**F3-07 关系映射定义**

- 为本体关系定义物理层面的关联条件
- 关系映射信息包含：
  - 源实体对应表/视图
  - 目标实体对应表/视图
  - 关联条件（JOIN ON 条件），如 `A.product_id = B.product_id`
  - 关系表名称（如果是多对多关系，需要中间关系表）
- 关系映射影响 DDL 生成和 Oracle Graph 构建

#### 3.3.4 LLM 映射 Prompt 设计要求

```
系统提示词（System Prompt）示例：

你是一个数据架构专家，擅长将业务本体模型映射到物理数据库表结构。
你的任务是根据给定的本体实体定义和数据库源表元数据，为每个本体属性找到最匹配的源表字段。

请按以下规则输出映射建议：
1. 优先匹配字段名称相似度（语义和字面）
2. 考虑字段 Comments 描述的语义匹配
3. 考虑数据类型的兼容性
4. 如果没有直接匹配的字段，建议计算映射方案
5. 为每个映射提供置信度（high/medium/low）和匹配理由

输出格式为 JSON：
{
  "mappings": [
    {
      "propertyName": "本体属性名",
      "sourceTable": "推荐源表名",
      "sourceColumn": "推荐源字段名",
      "mappingType": "direct|computed|constant",
      "confidence": "high|medium|low",
      "reason": "匹配理由",
      "formula": "计算公式表达式（当 mappingType=computed 时）"
    }
  ]
}
```

---

### 3.4 模块四：本体对象 DDL 生成与应用

#### 3.4.1 功能概述

基于映射完成后的本体定义，使用大模型自动生成创建本体对象表/视图的 DDL 语句，以及 Oracle Graph 构建的 DDL 语句。用户可检查、修改 DDL，确认后在 Oracle 数据库中执行创建。

#### 3.4.2 功能清单

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|----------|----------|--------|
| F4-01 | DDL 自动生成 | 基于 ontology 定义和映射配置，调用 LLM 生成 DDL | P0 |
| F4-02 | 实体 DDL 生成 | 生成实体表/视图的 CREATE 语句（含字段、类型、Comments） | P0 |
| F4-03 | 关系表 DDL 生成 | 生成关系表（中间表）的 CREATE 语句 | P0 |
| F4-04 | Oracle Graph DDL 生成 | 生成 Oracle Property Graph 构建 DDL | P0 |
| F4-05 | DDL 检查与编辑 | 用户在代码编辑器中查看和修改 DDL 内容 | P0 |
| F4-06 | DDL 执行应用 | 确认 DDL 后在 Oracle 数据库中执行创建实际对象 | P0 |
| F4-07 | DDL 执行日志 | 记录 DDL 执行结果（成功/失败、错误信息） | P0 |
| F4-08 | DDL 版本管理 | （扩展）支持 DDL 版本管理与回滚 | P2 |

#### 3.4.3 详细要求

**F4-01 DDL 自动生成**

- 系统汇总以下信息作为 LLM 输入：
  - 本体实体定义（名称、属性、描述）
  - 实体构建方式（View / Table）
  - 属性映射信息（源表、源字段、计算公式）
  - 关系定义及关系映射
- LLM 生成完整的 DDL 脚本，包含：
  - 实体 DDL（CREATE TABLE / CREATE VIEW）
  - 关系表 DDL（如有多对多关系）
  - Comments DDL（COMMENT ON TABLE / COMMENT ON COLUMN）
  - 索引 DDL（可选）
  - Oracle Graph DDL

**F4-02 实体 DDL 生成**

**Management Table 方式示例：**

```sql
-- 实体: DefectRecord (缺陷记录)
-- 构建方式: Management Table
CREATE TABLE ONTO_DEFECT_RECORD (
    DEFECT_ID      NUMBER(10)    NOT NULL,
    PRODUCT_ID     NUMBER(10)    NOT NULL,
    DEFECT_TYPE    VARCHAR2(50),
    DEFECT_DESC    VARCHAR2(500),
    DEFECT_TIME    DATE,
    PROCESS_ID     NUMBER(10),
    DEFECT_RATE    NUMBER(10,4)  -- 计算字段: 缺陷率
        GENERATED ALWAYS AS (
            CAST(DEFECT_COUNT AS FLOAT) / NULLIF(TOTAL_COUNT, 0) * 100
        ) VIRTUAL,
    CREATED_AT     TIMESTAMP DEFAULT SYSTIMESTAMP,
    CONSTRAINT PK_ONTO_DEFECT_RECORD PRIMARY KEY (DEFECT_ID)
);

COMMENT ON TABLE ONTO_DEFECT_RECORD IS '缺陷记录 - 记录生产过程中的缺陷信息';
COMMENT ON COLUMN ONTO_DEFECT_RECORD.DEFECT_ID IS '缺陷ID，主键';
COMMENT ON COLUMN ONTO_DEFECT_RECORD.PRODUCT_ID IS '产品ID，关联产品信息表';
COMMENT ON COLUMN ONTO_DEFECT_RECORD.DEFECT_TYPE IS '缺陷类型（划伤、凹陷、裂纹等）';
```

**Management View 方式示例：**

```sql
-- 实体: DefectRecord (缺陷记录)
-- 构建方式: Management View
CREATE OR REPLACE VIEW ONTO_DEFECT_RECORD_V AS
SELECT
    d.defect_id       AS defect_id,
    d.product_id      AS product_id,
    d.defect_type     AS defect_type,
    d.defect_desc     AS defect_desc,
    d.defect_time     AS defect_time,
    d.process_id      AS process_id,
    CAST(d.defect_count AS FLOAT) / NULLIF(p.total_count, 0) * 100 AS defect_rate
FROM
    DEFECT_RECORDS d
    LEFT JOIN PRODUCT_INFO p ON d.product_id = p.product_id;

COMMENT ON TABLE ONTO_DEFECT_RECORD_V IS '缺陷记录视图 - 记录生产过程中的缺陷信息';
```

**F4-03 关系表 DDL 生成**

```sql
-- 关系表: DefectRecord -> DefectType (导致关系，多对多)
CREATE TABLE ONTO_REL_DEFECT_TYPE (
    REL_ID          NUMBER(10) GENERATED ALWAYS AS IDENTITY PRIMARY KEY,
    DEFECT_ID       NUMBER(10) NOT NULL,
    DEFECT_TYPE_ID  NUMBER(10) NOT NULL,
    RELATION_NAME   VARCHAR2(100) DEFAULT '导致',
    RELATION_DESC   VARCHAR2(500),
    CREATED_AT      TIMESTAMP DEFAULT SYSTIMESTAMP,
    CONSTRAINT FK_REL_DEFECT FOREIGN KEY (DEFECT_ID)
        REFERENCES ONTO_DEFECT_RECORD(DEFECT_ID),
    CONSTRAINT FK_REL_TYPE FOREIGN KEY (DEFECT_TYPE_ID)
        REFERENCES ONTO_DEFECT_TYPE(DEFECT_TYPE_ID)
);

COMMENT ON TABLE ONTO_REL_DEFECT_TYPE IS '关系表: 缺陷记录 -> 缺陷类型 (导致关系)';
```

**F4-04 Oracle Graph DDL 生成**

```sql
-- Oracle Property Graph DDL
-- 创建属性图: PRODUCTION_DEFECT_GRAPH

BEGIN
    -- 创建图
    EXECUTE IMMEDIATE 'CREATE PROPERTY GRAPH production_defect_graph
        VERTEX TABLES (
            ONTO_DEFECT_RECORD AS DefectRecord
                KEY (DEFECT_ID)
                PROPERTIES (DEFECT_ID, PRODUCT_ID, DEFECT_TYPE, DEFECT_DESC, DEFECT_TIME),
            ONTO_DEFECT_TYPE AS DefectType
                KEY (DEFECT_TYPE_ID)
                PROPERTIES (DEFECT_TYPE_ID, TYPE_NAME, TYPE_DESC),
            ONTO_PRODUCT_INFO AS Product
                KEY (PRODUCT_ID)
                PROPERTIES (PRODUCT_ID, PRODUCT_NAME, PRODUCT_LINE),
            ONTO_PROCESS_INFO AS Process
                KEY (PROCESS_ID)
                PROPERTIES (PROCESS_ID, PROCESS_NAME, PROCESS_STEP)
        )
        EDGE TABLES (
            ONTO_REL_DEFECT_TYPE AS CAUSED_BY
                SOURCE KEY (DEFECT_ID) REFERENCES DefectRecord (DEFECT_ID)
                DESTINATION KEY (DEFECT_TYPE_ID) REFERENCES DefectType (DEFECT_TYPE_ID)
                PROPERTIES (RELATION_DESC),
            ONTO_REL_DEFECT_PROCESS AS PRODUCED_IN
                SOURCE KEY (DEFECT_ID) REFERENCES DefectRecord (DEFECT_ID)
                DESTINATION KEY (PROCESS_ID) REFERENCES Process (PROCESS_ID)
                PROPERTIES (RELATION_DESC)
        )';
END;
/
```

**F4-05 DDL 检查与编辑**

- 提供代码编辑器（如 Monaco Editor / CodeMirror），支持 SQL 语法高亮
- DDL 按实体分组展示，用户可折叠/展开
- 用户可直接编辑 DDL 文本
- 编辑后可保存为草稿
- 支持 DDL 全文搜索

**F4-06 DDL 执行应用**

- 用户点击"应用 DDL"按钮后：
  1. 系统弹出确认对话框，列出将要执行的 DDL 语句清单
  2. 用户确认后，系统按顺序执行 DDL
  3. 每条 DDL 执行结果实时反馈（成功/失败）
  4. 如执行失败，显示 Oracle 错误信息
  5. 支持选择"执行全部"或"逐条执行"
- 执行前自动检查目标对象是否已存在，如已存在提示用户选择：
  - 跳过（保留已有对象）
  - 替换（DROP + CREATE）
  - 取消

**F4-07 DDL 执行日志**

- 记录每次 DDL 执行的：
  - 执行时间
  - 执行人
  - DDL 内容
  - 执行结果（成功/失败）
  - 错误信息（如有）
  - 执行耗时
- 日志可在界面查询查看

---

### 3.5 模块五：本体对象浏览管理

#### 3.5.1 功能概述

对已构建到数据库中的本体对象进行浏览、查看和管理。支持图谱方式浏览、节点详情查看、Comments 修改、属性增改、关系增改等操作。

按照**业务分析域**进行管理。

#### 3.5.2 功能清单

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|----------|----------|--------|
| F5-01 | 本体图谱浏览 | 查询数据库中已构建的本体对象和关系，以图谱方式展示 | P0 |
| F5-02 | 节点详情查看 | 点击图谱节点，查看节点描述、属性字段描述 | P0 |
| F5-03 | 节点 Comments 修改 | 修改节点（表/视图）的 Comments，写回数据库 | P0 |
| F5-04 | 属性 Comments 修改 | 修改属性字段的 Comments，写回数据库 | P0 |
| F5-05 | 属性新增 | 为已构建节点添加新的属性字段 | P0 |
| F5-06 | 属性修改 | 修改已构建节点的属性（选择源表字段、计算方法） | P0 |
| F5-07 | 关系新增 | 在已构建节点之间添加新的关系，选择关系条件 | P0 |
| F5-08 | 关系修改 | 修改已有关系的条件 | P1 |
| F5-09 | 节点数据预览 | 预览已构建本体节点（表/视图）的数据 | P1 |

#### 3.5.3 详细要求

**F5-01 本体图谱浏览**

- 从数据库读取已构建的本体对象（`ONTO_*` 前缀的表/视图）和关系表
- 以图谱方式可视化展示：
  - 节点 = 本体实体（表/视图）
  - 边 = 关系（关系表）
  - 节点颜色/图标区分构建方式（View = 蓝色，Table = 绿色）
- 支持图谱交互：缩放、平移、全屏、导出
- 支持按分析域筛选展示
- 支持节点搜索定位

**F5-02 / F5-03 / F5-04 节点详情查看与 Comments 修改**

- 点击图谱节点后，在侧边面板展示：
  - 节点名称（表名/视图名）
  - 节点描述（表 Comments）
  - 构建方式（View/Table）
  - 属性列表：字段名、数据类型、字段 Comments、映射来源
- 节点描述可编辑（修改表 Comments），保存后执行 `COMMENT ON TABLE`
- 属性字段 Comments 可编辑，保存后执行 `COMMENT ON COLUMN`
- 修改后实时刷新界面显示

**F5-05 / F5-06 属性新增与修改**

**属性新增：**
- 用户在节点属性面板点击"添加属性"
- 选择属性来源：
  - 选择源表 → 选择源字段（直接映射）
  - 或输入计算公式（计算映射）
- 定义属性名称、数据类型、描述
- 系统生成 `ALTER TABLE ADD COLUMN` 或重建视图的 DDL
- 用户确认后执行 DDL
- 同步更新本体元数据表中的属性定义

**属性修改：**
- 选中已有属性，修改其映射来源或计算方法
- 系统生成对应的 `ALTER TABLE MODIFY` 或重建视图 DDL
- 用户确认后执行

**F5-07 / F5-08 关系新增与修改**

**关系新增：**
- 在图谱上选择两个节点，拉有向箭头
- 定义关系信息：
  - 关系名称
  - 关系类型
  - 关系描述
  - 关联条件（JOIN ON）
- 系统生成关系表 DDL（如多对多）或外键约束 DDL
- 执行 DDL 创建关系
- 同步更新 Oracle Graph（如需要）

**关系修改：**
- 选中已有关系边，修改关系条件
- 生成 ALTER DDL 执行

---

### 3.6 模块六：系统管理

#### 3.6.1 功能概述

提供系统级的配置和管理功能，包括大模型配置管理和用户管理。

#### 3.6.2 功能清单

| 编号 | 功能名称 | 功能描述 | 优先级 |
|------|----------|----------|--------|
| F6-01 | 大模型配置管理 | 管理 OpenAI 兼容大模型的连接配置信息 | P0 |
| F6-02 | 大模型测试连接 | 测试大模型配置是否可用 | P0 |
| F6-03 | 大模型配置切换 | 切换当前使用的大模型配置 | P0 |
| F6-04 | 用户管理 | 基础的用户增删改查、启用/禁用 | P0 |
| F6-05 | 用户密码修改 | 用户修改自己的登录密码 | P0 |
| F6-06 | 操作日志查看 | 查看系统操作日志 | P1 |

#### 3.6.3 详细要求

**F6-01 大模型配置管理**

- 大模型配置信息包含：

| 配置项 | 说明 | 示例 |
|--------|------|------|
| 配置名称 | 配置标识名称 | `生产环境-GPT4` |
| API Base URL | 大模型服务地址 | `https://api.openai.com/v1` |
| API Key | API 密钥（加密存储） | `sk-xxxx` |
| 模型名称 | 使用的模型标识 | `gpt-4o` / `deepseek-chat` |
| 请求超时 | 请求超时时间（秒） | `60` |
| 最大 Token | 单次请求最大 Token 数 | `4096` |
| 温度参数 | Temperature | `0.7` |
| 是否启用 | 启用/禁用状态 | `true` |
| 是否默认 | 是否为默认使用的配置 | `true` |

- 支持 OpenAI 兼容接口的大模型服务（OpenAI、Azure OpenAI、DeepSeek、通义千问等）
- API Key 在数据库中加密存储，界面展示时脱敏
- 支持多个大模型配置并存，可设置一个为默认
- 修改配置后，后续的大模型调用使用最新配置

**F6-02 大模型测试连接**

- 点击"测试连接"按钮，系统向大模型发送一条简单的测试消息
- 返回测试结果：
  - 成功：显示响应内容和耗时
  - 失败：显示错误信息（连接超时、认证失败、模型不存在等）

**F6-04 用户管理**

- 用户信息包含：用户名、显示名称、邮箱、角色、状态（启用/禁用）、创建时间
- 管理员可创建用户、编辑用户信息、重置密码、启用/禁用用户
- 用户角色（第一阶段简化）：
  - `admin` — 系统管理员，拥有全部权限
  - `analyst` — 业务分析师，可进行本体构建、映射、浏览操作
  - `viewer` — 只读用户，仅可浏览

**F6-05 用户密码修改**

- 用户可在个人设置中修改自己的密码
- 需输入当前密码 + 新密码 + 确认新密码
- 密码强度要求：至少8位，包含字母和数字
- 密码在数据库中以哈希方式存储（bcrypt）

---

## 4. Agent 智能体与 Skill 设计

### 4.1 总体架构（规划中）

```
┌──────────────────────────────────────────────────────────────┐
│                    Agent 智能体编排引擎                         │
├──────────────────────────────────────────────────────────────┤
│                                                              │
│   用户请求: "分析本次冲压工序的缺陷根因"                          │
│         │                                                    │
│         ▼                                                    │
│  ┌─────────────┐                                            │
│  │ Agent 核心   │ ── 意图识别 → 任务分解 → Skill 调度           │
│  └──────┬──────┘                                            │
│         │                                                    │
│    ┌────┼────────────┬─────────────┐                        │
│    ▼    ▼            ▼             ▼                        │
│ ┌──────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐             │
│ │Skill │ │Skill     │ │Skill     │ │Skill     │             │
│ │缺陷   │ │根因      │ │影响      │ │数据查询   │             │
│ │分析   │ │分析      │ │分析      │ │          │             │
│ └──┬───┘ └────┬─────┘ └────┬─────┘ └────┬─────┘             │
│    │         │            │             │                    │
│    └─────────┴────────────┴─────────────┘                    │
│                    │                                         │
│                    ▼                                         │
│         ┌───────────────────┐                                │
│         │  Oracle 26ai 图谱  │                                │
│         │  + 本体实例数据     │                                │
│         └───────────────────┘                                │
│                                                              │
└──────────────────────────────────────────────────────────────┘
```

### 4.2 第一阶段预留接口

第一阶段重点完成本体构建，但需为后续 Agent + Skill 分析能力预留以下接口和设计：

| 预留项 | 说明 |
|--------|------|
| 本体图谱查询接口 | 基于 Oracle Graph 的图查询 API，供后续 Skill 调用 |
| 分析流程定义数据 | 流程定义 JSON 已存储，后续由流程引擎解析执行 |
| LLM 调用服务 | 统一的大模型调用服务，后续 Agent 和 Skill 复用 |
| 本体元数据 API | 本体定义、属性、关系、映射信息的查询 API |
| Action 配置数据 | Action 定义已存储，后续由执行引擎调用 |

### 4.3 后续 Skill 规划（参考）

| Skill 名称 | 功能描述 | 依赖 |
|------------|----------|------|
| `defect_analysis` | 缺陷分析：对缺陷数据进行分类、统计、趋势分析 | 本体图谱 + 源数据 |
| `root_cause_analysis` | 根因分析：基于本体关系追溯缺陷根因 | 本体图谱 + Oracle Graph 查询 |
| `impact_analysis` | 影响分析：评估缺陷对上下游的影响范围 | 本体图谱 + 关系遍历 |
| `data_query` | 数据查询：从本体实例表/视图中查询数据 | 本体实例数据 |
| `report_generation` | 报告生成：将分析结果生成为结构化报告 | LLM + 分析结果 |

---

## 5. 非功能性需求

### 5.1 性能需求

| 指标 | 要求 |
|------|------|
| 页面加载时间 | 首屏加载 ≤ 3 秒（常规网络环境） |
| API 响应时间 | 普通查询接口 ≤ 1 秒；DDL 执行 ≤ 30 秒 |
| 图谱渲染 | 100 个节点以内 ≤ 2 秒完成渲染 |
| 源表数据查询 | 单次查询返回 ≤ 1000 条记录时 ≤ 2 秒 |
| LLM 响应 | 单次 LLM 调用 ≤ 60 秒（超时可配置） |
| 并发用户 | 支持至少 50 个并发用户同时在线操作 |

### 5.2 安全性需求

| 需求项 | 要求 |
|--------|------|
| 身份认证 | 基于 JWT 的身份认证，Token 有效期可配置 |
| 密码安全 | 密码使用 bcrypt 哈希存储，禁止明文存储 |
| API Key 安全 | 大模型 API Key 加密存储，界面脱敏展示 |
| SQL 注入防护 | 所有数据库操作使用参数化查询，禁止字符串拼接 SQL |
| 操作审计 | 关键操作（DDL 执行、Comments 修改、用户管理）记录操作日志 |
| 权限控制 | 基于角色的访问控制（RBAC），不同角色可访问不同功能 |
| 数据安全 | 源数据只读，平台不修改客户业务表数据（Comments 除外） |

### 5.3 可用性需求

| 需求项 | 要求 |
|--------|------|
| 浏览器兼容 | 支持 Chrome 90+、Edge 90+、Firefox 88+ |
| 响应式设计 | 适配 1280px 及以上屏幕分辨率 |
| 错误提示 | 所有操作失败时提供明确的错误提示信息 |
| 操作确认 | 危险操作（DDL 执行、删除）需二次确认 |
| 数据校验 | 前端 + 后端双重数据校验 |

### 5.4 可维护性需求

| 需求项 | 要求 |
|--------|------|
| 代码规范 | 前端遵循 ESLint + Prettier 规范；后端遵循 PEP 8 |
| API 文档 | FastAPI 自动生成 OpenAPI 文档，保持与代码同步 |
| 日志 | 后端使用结构化日志（JSON 格式），支持日志级别控制 |
| 配置管理 | 数据库连接、大模型配置等通过环境变量 / 配置文件管理 |
| 版本管理 | 代码使用 Git 管理，遵循 Git Flow 分支策略 |

---

## 6. 数据库设计要求

### 6.1 数据库总体结构

```
Oracle 26ai 数据库
├── 客户源数据 Schema（客户提供）
│   ├── DEFECT_RECORDS          -- 缺陷记录表
│   ├── PRODUCT_INFO            -- 产品信息表
│   ├── PROCESS_LOG             -- 工序日志表
│   └── ...                     -- 其他业务表
│
├── 平台管理 Schema（平台自建）
│   ├── SYS_DOMAIN              -- 业务分析域
│   ├── SYS_ONTOLOGY_ENTITY     -- 本体实体定义
│   ├── SYS_ONTOLOGY_PROPERTY   -- 本体属性定义
│   ├── SYS_ONTOLOGY_RELATION   -- 本体关系定义
│   ├── SYS_ENTITY_MAPPING      -- 实体数据映射配置
│   ├── SYS_PROPERTY_MAPPING    -- 属性数据映射配置
│   ├── SYS_RELATION_MAPPING    -- 关系映射配置
│   ├── SYS_DDL_LOG             -- DDL 执行日志
│   ├── SYS_PROCESS_DEF         -- 分析流程定义（JSON存储）
│   ├── SYS_LLM_CONFIG          -- 大模型配置
│   ├── SYS_USER                -- 用户信息
│   ├── SYS_OPERATION_LOG       -- 操作日志
│   └── ...
│
├── 本体实例 Schema（DDL 生成）
│   ├── ONTO_DEFECT_RECORD      -- 缺陷记录（表或视图）
│   ├── ONTO_DEFECT_TYPE        -- 缺陷类型
│   ├── ONTO_PRODUCT_INFO       -- 产品信息
│   ├── ONTO_PROCESS_INFO       -- 工序信息
│   ├── ONTO_REL_*              -- 关系表
│   └── ...
│
└── Oracle Graph
    └── PRODUCTION_DEFECT_GRAPH -- 生产缺陷属性图
```

### 6.2 核心元数据表设计

#### 6.2.1 SYS_DOMAIN（业务分析域）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| DOMAIN_ID | VARCHAR2(50) PK | 分析域ID |
| DOMAIN_NAME | VARCHAR2(200) | 分析域名称 |
| DOMAIN_DESC | VARCHAR2(1000) | 分析域描述 |
| STATUS | VARCHAR2(20) | 状态（ACTIVE/INACTIVE） |
| CREATED_BY | VARCHAR2(50) | 创建人 |
| CREATED_AT | TIMESTAMP | 创建时间 |
| UPDATED_AT | TIMESTAMP | 更新时间 |

#### 6.2.2 SYS_ONTOLOGY_ENTITY（本体实体定义）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| ENTITY_ID | VARCHAR2(50) PK | 实体ID |
| DOMAIN_ID | VARCHAR2(50) FK | 所属分析域 |
| ENTITY_NAME | VARCHAR2(100) | 实体名称（英文） |
| ENTITY_DISPLAY_NAME | VARCHAR2(200) | 显示名称（中文） |
| ENTITY_DESC | VARCHAR2(1000) | 实体描述 |
| BUILD_TYPE | VARCHAR2(20) | 构建方式（VIEW/TABLE） |
| TABLE_NAME | VARCHAR2(100) | 对应物理表/视图名 |
| STATUS | VARCHAR2(20) | 状态（DRAFT/MAPPED/DDL_GENERATED/DEPLOYED） |
| GRAPH_POSITION | CLOB | 图形位置信息（JSON） |
| CREATED_BY | VARCHAR2(50) | 创建人 |
| CREATED_AT | TIMESTAMP | 创建时间 |
| UPDATED_AT | TIMESTAMP | 更新时间 |

#### 6.2.3 SYS_ONTOLOGY_PROPERTY（本体属性定义）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| PROPERTY_ID | VARCHAR2(50) PK | 属性ID |
| ENTITY_ID | VARCHAR2(50) FK | 所属实体 |
| PROPERTY_NAME | VARCHAR2(100) | 属性名称（英文） |
| PROPERTY_DISPLAY_NAME | VARCHAR2(200) | 显示名称（中文） |
| DATA_TYPE | VARCHAR2(50) | 数据类型 |
| IS_PRIMARY_KEY | CHAR(1) | 是否主键（Y/N） |
| IS_NULLABLE | CHAR(1) | 是否可空（Y/N） |
| PROPERTY_DESC | VARCHAR2(500) | 属性描述 |
| ORDER_NUM | NUMBER(5) | 排序序号 |
| CREATED_AT | TIMESTAMP | 创建时间 |
| UPDATED_AT | TIMESTAMP | 更新时间 |

#### 6.2.4 SYS_ONTOLOGY_RELATION（本体关系定义）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| RELATION_ID | VARCHAR2(50) PK | 关系ID |
| DOMAIN_ID | VARCHAR2(50) FK | 所属分析域 |
| SOURCE_ENTITY_ID | VARCHAR2(50) FK | 源实体ID |
| TARGET_ENTITY_ID | VARCHAR2(50) FK | 目标实体ID |
| RELATION_NAME | VARCHAR2(100) | 关系名称 |
| RELATION_TYPE | VARCHAR2(50) | 关系类型（ONE_TO_ONE/ONE_TO_MANY/MANY_TO_MANY/INHERITANCE/ASSOCIATION） |
| RELATION_DESC | VARCHAR2(1000) | 关系描述 |
| RELATION_TABLE_NAME | VARCHAR2(100) | 关系表名（多对多时） |
| CREATED_AT | TIMESTAMP | 创建时间 |
| UPDATED_AT | TIMESTAMP | 更新时间 |

#### 6.2.5 SYS_PROPERTY_MAPPING（属性映射配置）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| MAPPING_ID | VARCHAR2(50) PK | 映射ID |
| PROPERTY_ID | VARCHAR2(50) FK | 本体属性ID |
| SOURCE_TABLE | VARCHAR2(100) | 源表名 |
| SOURCE_COLUMN | VARCHAR2(100) | 源字段名 |
| MAPPING_TYPE | VARCHAR2(20) | 映射类型（DIRECT/COMPUTED/CONSTANT/LLM_DERIVED） |
| FORMULA_EXPR | VARCHAR2(2000) | 计算公式表达式（SQL） |
| FORMULA_DESC | VARCHAR2(1000) | 计算公式描述（自然语言） |
| CONFIDENCE | VARCHAR2(10) | 置信度（HIGH/MEDIUM/LOW，LLM映射时） |
| MAPPING_STATUS | VARCHAR2(20) | 映射状态（PENDING/CONFIRMED/REJECTED） |
| MAPPED_BY | VARCHAR2(50) | 映射人 |
| MAPPED_AT | TIMESTAMP | 映射时间 |

#### 6.2.6 SYS_PROCESS_DEF（分析流程定义）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| PROCESS_ID | VARCHAR2(50) PK | 流程ID |
| DOMAIN_ID | VARCHAR2(50) FK | 所属分析域 |
| PROCESS_NAME | VARCHAR2(200) | 流程名称 |
| PROCESS_DESC | VARCHAR2(1000) | 流程描述 |
| PROCESS_JSON | CLOB | 流程定义JSON |
| VERSION | VARCHAR2(20) | 版本号 |
| STATUS | VARCHAR2(20) | 状态（DRAFT/PUBLISHED） |
| CREATED_BY | VARCHAR2(50) | 创建人 |
| CREATED_AT | TIMESTAMP | 创建时间 |
| UPDATED_AT | TIMESTAMP | 更新时间 |

#### 6.2.7 SYS_LLM_CONFIG（大模型配置）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| CONFIG_ID | VARCHAR2(50) PK | 配置ID |
| CONFIG_NAME | VARCHAR2(100) | 配置名称 |
| API_BASE_URL | VARCHAR2(500) | API地址 |
| API_KEY_ENC | VARCHAR2(500) | API Key（加密存储） |
| MODEL_NAME | VARCHAR2(100) | 模型名称 |
| TEMPERATURE | NUMBER(3,2) | 温度参数 |
| MAX_TOKENS | NUMBER(10) | 最大Token数 |
| TIMEOUT | NUMBER(5) | 超时时间（秒） |
| IS_ACTIVE | CHAR(1) | 是否启用 |
| IS_DEFAULT | CHAR(1) | 是否默认 |
| CREATED_AT | TIMESTAMP | 创建时间 |
| UPDATED_AT | TIMESTAMP | 更新时间 |

#### 6.2.8 SYS_USER（用户信息）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| USER_ID | VARCHAR2(50) PK | 用户ID |
| USERNAME | VARCHAR2(50) UK | 用户名（唯一） |
| DISPLAY_NAME | VARCHAR2(100) | 显示名称 |
| EMAIL | VARCHAR2(200) | 邮箱 |
| PASSWORD_HASH | VARCHAR2(200) | 密码哈希 |
| ROLE | VARCHAR2(20) | 角色（ADMIN/ANALYST/VIEWER） |
| STATUS | VARCHAR2(20) | 状态（ACTIVE/INACTIVE） |
| CREATED_AT | TIMESTAMP | 创建时间 |
| UPDATED_AT | TIMESTAMP | 更新时间 |

---

## 7. 接口规范

### 7.1 API 设计规范

- 遵循 RESTful API 设计规范
- 接口基础路径：`/api/v1`
- 请求/响应格式：JSON
- 认证方式：Bearer Token（JWT）
- 统一响应结构：

```json
{
  "code": 200,
  "message": "success",
  "data": { },
  "timestamp": "2026-07-13T10:00:00Z"
}
```

### 7.2 核心接口清单

| 模块 | 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|------|
| 源数据 | 获取表列表 | GET | `/api/v1/source/tables` | 获取Schema下表清单 |
| 源数据 | 获取表结构 | GET | `/api/v1/source/tables/{tableName}/columns` | 获取表字段信息 |
| 源数据 | 修改表Comments | PUT | `/api/v1/source/tables/{tableName}/comments` | 修改表Comments |
| 源数据 | 修改字段Comments | PUT | `/api/v1/source/tables/{tableName}/columns/{columnName}/comments` | 修改字段Comments |
| 源数据 | 查询表数据 | POST | `/api/v1/source/tables/{tableName}/data` | 分页查询表数据 |
| 本体 | 获取分析域列表 | GET | `/api/v1/domains` | 获取分析域列表 |
| 本体 | 创建分析域 | POST | `/api/v1/domains` | 创建分析域 |
| 本体 | 获取实体列表 | GET | `/api/v1/domains/{domainId}/entities` | 获取域下实体列表 |
| 本体 | 创建实体 | POST | `/api/v1/domains/{domainId}/entities` | 创建本体实体 |
| 本体 | 获取实体属性 | GET | `/api/v1/entities/{entityId}/properties` | 获取实体属性 |
| 本体 | 创建关系 | POST | `/api/v1/domains/{domainId}/relations` | 创建本体关系 |
| 本体 | 获取本体图 | GET | `/api/v1/domains/{domainId}/graph` | 获取本体关系图数据 |
| 本体 | 保存分析流程 | POST | `/api/v1/domains/{domainId}/processes` | 保存流程定义 |
| 映射 | 获取映射配置 | GET | `/api/v1/entities/{entityId}/mappings` | 获取实体映射配置 |
| 映射 | 保存映射配置 | PUT | `/api/v1/entities/{entityId}/mappings` | 保存映射配置 |
| 映射 | LLM自动映射 | POST | `/api/v1/entities/{entityId}/auto-mapping` | 调用LLM自动映射 |
| 映射 | 确认映射 | POST | `/api/v1/entities/{entityId}/mappings/confirm` | 确认映射结果 |
| DDL | 生成DDL | POST | `/api/v1/domains/{domainId}/ddl/generate` | 调用LLM生成DDL |
| DDL | 执行DDL | POST | `/api/v1/domains/{domainId}/ddl/execute` | 执行DDL |
| DDL | 获取DDL日志 | GET | `/api/v1/ddl/logs` | 获取DDL执行日志 |
| 浏览 | 获取已构建本体图 | GET | `/api/v1/domains/{domainId}/ontology-graph` | 获取已部署本体图谱 |
| 浏览 | 修改节点Comments | PUT | `/api/v1/ontology/tables/{tableName}/comments` | 修改已构建对象Comments |
| 浏览 | 添加属性 | POST | `/api/v1/ontology/tables/{tableName}/columns` | 添加属性字段 |
| 浏览 | 添加关系 | POST | `/api/v1/ontology/relations` | 添加新关系 |
| 系统 | 获取大模型配置 | GET | `/api/v1/system/llm-configs` | 获取大模型配置列表 |
| 系统 | 保存大模型配置 | POST | `/api/v1/system/llm-configs` | 保存大模型配置 |
| 系统 | 测试大模型连接 | POST | `/api/v1/system/llm-configs/{configId}/test` | 测试连接 |
| 系统 | 用户登录 | POST | `/api/v1/auth/login` | 用户登录 |
| 系统 | 修改密码 | PUT | `/api/v1/auth/password` | 修改密码 |
| 系统 | 用户管理 | GET/POST/PUT/DELETE | `/api/v1/system/users` | 用户CRUD |

---

## 8. 第一阶段交付物清单

| 序号 | 交付物 | 说明 |
|------|--------|------|
| 1 | 本需求规范书 | 本文档 |
| 2 | 系统原型设计 | 前端界面原型（Figma / Axure） |
| 3 | 数据库设计文档 | 详细的表结构设计文档 |
| 4 | API 接口文档 | OpenAPI / Swagger 格式 |
| 5 | 前端源代码 | Vue 3 项目代码 |
| 6 | 后端源代码 | Python FastAPI 项目代码 |
| 7 | DDL 脚本 | 平台管理表创建脚本 |
| 8 | 部署文档 | 系统部署安装指南 |
| 9 | 用户手册 | 系统操作使用手册 |
| 10 | 测试报告 | 功能测试与验收测试报告 |

---

## 附录 A：业务分析域示例

以**冲压缺陷分析域**为例，展示本体构建过程：

### A.1 本体实体

| 实体名称 | 显示名称 | 描述 | 构建方式 |
|----------|----------|------|----------|
| DefectRecord | 缺陷记录 | 记录生产过程中发现的缺陷信息 | Management Table |
| DefectType | 缺陷类型 | 缺陷分类定义（划伤、凹陷、裂纹等） | Management Table |
| Product | 产品信息 | 生产的产品基本信息 | Management View |
| Process | 工序信息 | 生产工序定义 | Management View |
| Material | 物料信息 | 使用的原材料信息 | Management View |
| Equipment | 设备信息 | 生产设备信息 | Management View |
| Operator | 操作人员 | 生产操作人员信息 | Management View |

### A.2 本体关系

| 源实体 | 关系名称 | 目标实体 | 关系类型 | 描述 |
|--------|----------|----------|----------|------|
| DefectRecord | 导致 | DefectType | 多对一 | 一个缺陷记录对应一种缺陷类型 |
| DefectRecord | 产生于 | Process | 多对一 | 缺陷在某个工序中产生 |
| DefectRecord | 影响到 | Product | 多对一 | 缺陷影响某个产品 |
| Process | 使用 | Equipment | 多对一 | 工序使用某台设备 |
| Process | 使用 | Material | 多对多 | 工序使用多种物料 |
| Process | 由 | Operator | 多对一 | 工序由某操作人员执行 |
| DefectType | 影响 | DefectType | 多对多 | 一种缺陷可能引发其他缺陷（级联影响） |

### A.3 分析流程示例

```
开始 → [缺陷数据查询] → [缺陷分类统计] → [根因追溯分析] → [影响范围评估] → [生成分析报告] → 结束
```

---

## 附录 B：Oracle 26ai 关键特性使用

| 特性 | 用途 |
|------|------|
| Property Graph | 构建本体关系图谱，支持图查询（PGQL）进行根因追溯和影响分析 |
| JSON | 存储分析流程定义、图形位置信息等半结构化数据 |
| Vector Embedding | （后续阶段）对缺陷描述进行向量化，支持语义相似度搜索 |
| SQL Macro | 封装常用分析查询逻辑，供 Skill 调用 |
| Autonomous Database | （可选）利用自治数据库的自动调优和扩展能力 |

---

*文档结束*
