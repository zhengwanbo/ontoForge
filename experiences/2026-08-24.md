# 2026-08-24

## 平台版本升级至 v1.2.0

### 背景

完成本体 Guide 生成优化、数据映射检查与 DDL 生成前结构化校验等功能后，将平台对外应用版本升级至 v1.2.0。

### 改动文件

- `backend/app/core/config.py`

### 实施内容

- 将 FastAPI 应用配置中的 `APP_VERSION` 从 `1.1.0` 更新为 `1.2.0`。

### 验证结果

- `cd backend && python -m py_compile app/core/config.py`：通过。
- `cd backend && python -c "from app.core.config import settings; assert settings.APP_VERSION == '1.2.0'"`：通过。
