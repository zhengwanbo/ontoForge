## GitHub 项目说明与初始发布

### 背景
- 项目由本地工作区首次上传至 GitHub，原始 `README.md` 仅包含项目名称，无法说明面向制造缺陷分析的本体构建能力、运行依赖与安全边界。

### 改动文件列表
- `.gitignore`
- `README.md`
- `experiences/2026-08-06.md`

### 具体改动
- 补充 Git 忽略规则，排除本地环境、Python 缓存、前端依赖、临时文件和本地数据库，避免将运行产物及潜在敏感数据提交到远程仓库。
- 新增中文 GitHub README，说明项目定位、核心功能、前后端和数据库架构、技术栈、Oracle 环境要求、启动步骤、推荐业务流程、验证命令、目录结构和安全提示。
- 初始提交已推送至 GitHub `zhengwanbo/ontoForge` 的 `main` 分支。

### 验证结果
- 已根据现有 FastAPI 路由、Vue 路由、依赖清单及最新阶段记录核对 README 所列功能与命令。
- 本次仅更新文档，未执行应用构建或后端测试。

### 遗留问题与下一步建议
- 可补充脱敏后的界面截图或架构图，以增强 GitHub 项目首页的可读性。
- 后续如确定开源方式，应添加明确的 LICENSE 文件与贡献指南。

## README 前后端架构与联调说明补充

### 背景
- 用户要求在 GitHub README 中进一步明确当前前后端技术架构，以及本地运行前端、后端和联调的方式。

### 改动文件列表
- `README.md`
- `experiences/2026-08-06.md`

### 具体改动
- 将架构说明细分为前端、后端、数据与集成层，明确 Vue/TypeScript/Vite、FastAPI/SQLAlchemy/Pydantic、Oracle Property Graph 与 LLM 的职责边界。
- 说明前端 Axios 使用 `/api/v1` 基址、Vite 将 `/api` 代理至 `http://localhost:8000`、JWT 在请求中自动携带的完整调用链。
- 将快速开始扩展为 Oracle 前置条件、后端启动、前端启动和联调检查，补充端口、健康检查、OpenAPI 地址与常见端口变更注意事项。

### 验证结果
- 已核对 `frontend/src/api/index.ts` 的 `/api/v1` 基址、`frontend/vite.config.ts` 的代理规则、`backend/app/core/config.py` 的 CORS 设置及 `backend/app/main.py` 的 API 挂载路径。

### 遗留问题与下一步建议
- 如部署到测试或生产环境，应在 Web 服务器/网关中配置静态资源托管和 `/api` 反向代理，并将实际域名加入 `CORS_ORIGINS`。
