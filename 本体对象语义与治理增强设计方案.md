# 本体对象语义与治理增强设计方案

## 1. 背景与目标

当前项目的本体建模能力已经具备以下基础：

- 后端已有实体、属性、关系三类核心模型，分别位于 `backend/app/models/models.py` 中的 `SysOntologyEntity`、`SysOntologyProperty`、`SysOntologyRelation`。
- 本体 CRUD API 已集中在 `backend/app/api/ontology.py`。
- 前端本体建模页面集中在 `frontend/src/views/ontology/OntologyBuild.vue`，支持实体、属性、关系的可视化维护。
- AI Guide 生成与应用能力已经接入 `backend/app/services/ontology_guide_service.py`，可批量生成并落库实体、属性、关系。

但当前模型仍偏“技术建模”，缺少面向语义引擎、分析查询和数据治理的业务元数据。现状中最明显的缺口有三类：

- 实体只有 `build_type=TABLE/VIEW`，没有“对象语义类型”，无法区分事实对象和维度对象。
- 属性只有 `data_type / is_primary_key / is_nullable / property_desc` 等基础信息，缺少单位、约束、用途、必带过滤、引用关系等业务语义。
- 实体没有治理信息承载位，无法沉淀口径归属、安全分级和更新频率。

本设计目标是在不推翻当前架构的前提下，补齐上述业务元数据，并让这些元数据能被以下链路消费：

- 本体对象/属性人工维护
- Guide 自动生成与应用
- 数据映射与关系 Join 推导
- 后续语义查询、分析推荐、治理检索

## 2. 现状分析

### 2.1 实体层现状

`SysOntologyEntity` 当前字段包括：

- `entity_name / entity_display_name / entity_desc`
- `build_type`：物理构建方式，当前是 `TABLE / VIEW`
- `status`：技术生命周期，当前表示 `DRAFT / MAPPED / DDL_GENERATED / DEPLOYED`
- `table_name / color / graph_position`

结论：

- `build_type` 表示“怎么落表”，不是“对象是什么”。
- `status` 已被技术流程占用，不能直接复用为治理状态。

### 2.2 属性层现状

`SysOntologyProperty` 当前字段包括：

- `property_name / property_display_name / property_desc`
- `data_type`
- `is_primary_key`
- `is_nullable`
- `order_num / source_mark`

结论：

- 已有 `is_primary_key`，这是后续属性语义扩展的一个起点。
- 但业务侧最关心的 `unit / constraint / usage / required_filter / ref` 尚未建模。

### 2.3 生成与映射链路现状

- `OntologyGuideService` 在应用预览时会直接创建/更新实体与属性。
- `mapping.py` 已经存在关系 Join 推荐、属性映射、边 SQL 生成能力。
- 当前关系 Join 候选主要依据：
  - 同名属性
  - 主键/标识列猜测
  - 蓝图中的 source hint

结论：

- `ref` 一旦补齐，不应只停留在展示层，应接入 Join 推荐排序逻辑。

## 3. 设计原则

1. 不混用已有字段语义。
`build_type` 继续表示物理构建方式；`status` 继续表示技术状态；新增字段承载语义和治理，不复用旧字段。

2. 优先增量改造现有实体表，而不是引入过度复杂的新表。
当前项目没有 Alembic，表结构演进依赖 `main.py` 中的 `ensure_*_columns()` 风格增量补列，实体级治理信息更适合先直接挂在 `sys_ontology_entity` 上。

3. 对多值字段使用 JSON 文本存储，对强约束字段使用独立列。
这与仓库中已有 `graph_position`、`process_json`、`source_tables_json` 的存储方式一致。

4. 先保证人工维护可用，再逐步让 Guide/映射链路消费这些元数据。

## 4. 目标能力范围

本次增强分三块：

### 4.1 实体对象类型

新增 `object_type`，用于区分：

- `FACT`：事实对象，参与聚合，如订单、退款、检测记录、工序事件
- `DIM`：维度对象，参与 Join、分组和筛选，如客户、门店、产线、设备

注意：

- `object_type` 与 `build_type` 正交
- 一个对象可以是 `FACT + VIEW`，也可以是 `DIM + TABLE`

### 4.2 属性业务语义

新增属性级业务元数据：

- `unit`：单位，例如 `CNY`、`PCS`、`ms`
- `value_constraint`：值域约束，先按可读表达式存储，例如 `>= 0`
- `usage_codes`：用途，建议支持多选，取值来自 `find / fetch / analyze`
- `is_required_filter`：是否为必带过滤字段
- `ref_property_id`：引用的目标本体属性
- `ref_display`：接口输出用的只读显示值，如 `Store.storeId`

说明：

- `is_primary_key` 保持沿用现有字段，不重复造一套 `primary_key`。
- `usage` 建议做多值，而不是单值。原因是同一字段可能既用于 `find`，也用于 `fetch`。

### 4.3 实体治理信息

新增实体级治理字段：

- `governance_status`：如 `ACTIVE / INACTIVE / DEPRECATED`
- `data_owner`：数据负责人或团队
- `security_level`：如 `L1 / L2 / L3`
- `update_frequency`：如 `REALTIME / T+1 / DAILY / WEEKLY`
- `governance_tags_json`：标签数组，例如 `["核心","财务"]`

## 5. 数据模型设计

### 5.1 `sys_ontology_entity` 增强

建议新增列：

- `object_type VARCHAR2(20)`
- `governance_status VARCHAR2(20)`
- `data_owner VARCHAR2(200)`
- `security_level VARCHAR2(20)`
- `update_frequency VARCHAR2(50)`
- `governance_tags_json CLOB`

说明：

- `object_type` 推荐允许空值用于兼容历史数据，但新建和编辑时前端要求必填。
- `governance_tags_json` 存 JSON 数组，避免一开始引入单独 tag 表。

### 5.2 `sys_ontology_property` 增强

建议新增列：

- `unit VARCHAR2(50)`
- `value_constraint VARCHAR2(500)`
- `usage_codes_json CLOB`
- `is_required_filter CHAR(1) DEFAULT 'N'`
- `ref_property_id VARCHAR2(50)`

说明：

- `ref_property_id` 语义上引用 `sys_ontology_property.property_id`。
- 当前仓库多数外键都由 ORM 管理，但 Oracle 线上变更复杂，本期可以先做“应用层约束 + ORM relationship”，不强依赖数据库级外键。
- `usage_codes_json` 例如 `["find","fetch"]`。

### 5.3 ORM 模型调整

#### `SysOntologyEntity`

在 `backend/app/models/models.py` 中增加对应列。

#### `SysOntologyProperty`

在 `backend/app/models/models.py` 中增加对应列，并补一个自关联关系：

- `ref_property_id`
- `referenced_property = relationship(...)`

是否一定要加 ORM relationship：

- 不是硬要求。
- 如果只做 API 组装，也可以先不加 relationship，采用查询时手动 join 的方式，改动面更小。

对本项目而言，推荐先不上复杂自关联 relationship，直接在 API 层拼装 `ref_display`，风险更低。

## 6. API 设计

### 6.1 Schema 变更

需要修改 `backend/app/schemas/schemas.py` 中以下结构：

- `EntityCreate`
- `EntityUpdate`
- `EntityResponse`
- `PropertyCreate`
- `PropertyUpdate`
- `PropertyResponse`

建议字段定义：

#### 实体

- `object_type: str`
- `governance_status: Optional[str]`
- `data_owner: Optional[str]`
- `security_level: Optional[str]`
- `update_frequency: Optional[str]`
- `governance_tags: List[str] = []`

#### 属性

- `unit: Optional[str]`
- `value_constraint: Optional[str]`
- `usage_codes: List[str] = []`
- `is_required_filter: str = "N"`
- `ref_property_id: Optional[str]`
- `ref_display: Optional[str] = None`

### 6.2 现有接口兼容策略

优先复用已有接口，不新增一套平行 API：

- `POST /domains/{domain_id}/entities`
- `PUT /entities/{entity_id}`
- `GET /domains/{domain_id}/entities`
- `POST /entities/{entity_id}/properties`
- `PUT /properties/{property_id}`
- `GET /entities/{entity_id}/properties`

原因：

- 当前前端和 Guide 应用逻辑都已经围绕这套接口构建。
- 扩展字段即可完成需求，不需要拆出专门的治理服务。

### 6.3 后端校验规则

#### 实体校验

- `object_type` 仅允许 `FACT / DIM`
- `governance_status` 允许空；如不空则限定枚举

#### 属性校验

- `usage_codes` 仅允许 `find / fetch / analyze`
- `is_required_filter` 仅允许 `Y / N`
- `ref_property_id` 如果非空，则必须满足：
  - 引用属性存在
  - 引用属性属于同一 `domain_id`
  - 不允许引用自己

建议增加一条业务约束：

- 若 `ref_property_id` 非空，被引用属性最好是主键或业务唯一键。
- 本期先做“非强制校验 + warning 文案”；不要因为存量数据不完美把流程彻底卡死。

## 7. 前端交互设计

核心改造文件：`frontend/src/views/ontology/OntologyBuild.vue`

### 7.1 实体编辑弹窗

现有实体弹窗只有名称、显示名、描述、构建方式、颜色。

建议改为两个分组：

- 基础信息
- 治理信息

新增控件：

- `对象类型`：下拉框，`事实 FACT / 维度 DIM`
- `治理状态`：下拉框
- `数据负责人`：输入框
- `安全级别`：下拉框
- `更新频率`：下拉框或可输入选择
- `标签`：可新增/删除标签的多值输入

### 7.2 右侧属性面板

当前右侧面板只展示：

- 属性名
- 数据类型
- PK 标识
- 简短说明

建议增强为轻量摘要展示：

- 主键 `PK`
- 单位 `unit`
- 用途标签，如 `find`、`analyze`
- `必带过滤` 标签
- `ref` 摘要，如 `-> Store.storeId`

### 7.3 属性编辑弹窗

当前属性弹窗仅支持：

- 属性名
- 显示名
- 数据类型
- 是否主键
- 是否可空
- 描述

建议新增：

- `单位`
- `值域约束`
- `用途`：多选
- `必带过滤`：开关
- `引用属性`：下拉搜索，候选值来自当前分析域全部实体属性

候选展示格式：

- `Store.storeId | 门店ID`
- `Customer.customerId | 客户ID`

### 7.4 图节点摘要

图上的实体节点建议加一行类型信息，避免用户把 `build_type` 误认成业务类型：

- 当前：`VIEW | 属性: 5`
- 建议：`FACT | VIEW | 属性: 5`

## 8. Guide 自动生成联动

核心改造文件：`backend/app/services/ontology_guide_service.py`

### 8.1 预览结构扩展

Guide 生成的 entity payload 增加：

- `objectType`
- `governanceInfo`（可选，本期可不由 AI 生成）

Guide 生成的 property payload 增加：

- `unit`
- `valueConstraint`
- `usage`
- `isRequiredFilter`
- `ref`

### 8.2 应用落库逻辑扩展

当前 `apply_preview_to_domain()` 在 upsert 实体/属性时仅处理基础字段。

需要补齐：

- 实体的 `object_type` 与治理字段写入
- 属性的 `unit / value_constraint / usage_codes_json / is_required_filter / ref_property_id` 写入

### 8.3 生成策略建议

`object_type` 可以纳入 Guide 推断，且价值很高：

- 包含事件、单据、记录、流水、交易、检测结果等语义的对象，优先判为 `FACT`
- 包含客户、门店、设备、物料、产线、班组等主数据语义的对象，优先判为 `DIM`

治理信息不建议在本期完全交给模型生成：

- `data_owner / security_level / update_frequency` 更接近组织制度，不适合默认自动填真值
- 本期可只生成空模板，由用户确认

## 9. 数据映射与 Join 推导联动

核心改造文件：`backend/app/api/mapping.py`

### 9.1 `ref` 对 Join 分析的作用

当前 `_relation_join_candidates()` 主要依据同名属性和常见标识列猜测。

增强后可增加一条高优先级规则：

- 如果源实体某属性 `A.ref_property_id = B.property_id`
- 且 `A` 属于关系源实体、`B` 属于关系目标实体
- 则将 `A.source_column = B.source_column` 作为高优先级 Join 候选

效果：

- `Order.storeId -> Store.storeId`
- `Order.customerId -> Customer.customerId`

这类典型业务外键不必再完全依赖列名偶然一致或人工试错。

### 9.2 `object_type` 对映射校验的作用

本期不建议在 DDL 或映射阶段对 `FACT / DIM` 做硬阻断，但建议增加 warning：

- `FACT` 对象没有 `analyze` 用途的属性时给出提示
- `DIM` 对象没有主键或没有可作为 `find/fetch` 的标识属性时给出提示

## 10. 语义引擎与查询消费建议

虽然当前仓库里还没有一个独立命名为“语义引擎”的模块，但这些元数据未来至少应服务以下能力：

- 查询推荐
- 口径解释
- Agent 提示词拼装
- Graph Query 推荐

建议消费规则如下：

- `FACT`：默认作为指标和聚合来源
- `DIM`：默认作为筛选与分组来源
- `usage=find`：优先用于精确定位对象
- `usage=fetch`：优先用于详情展示
- `usage=analyze`：优先用于指标、趋势、聚合
- `is_required_filter=Y`：查询生成时提示用户或自动补充筛选约束
- `security_level`：用于后续结果脱敏或访问控制扩展

## 11. 数据迁移与发布方案

### 11.1 表结构迁移

由于项目当前没有 Alembic，建议沿用 `backend/app/main.py` 里的增量补列模式，新增：

- `ensure_ontology_entity_semantic_columns()`
- `ensure_ontology_property_semantic_columns()`

分别执行 `ALTER TABLE ... ADD ...`

### 11.2 历史数据兼容

建议分两步：

1. 发布第一版时允许历史记录字段为空
2. 前端对新建/编辑数据要求填写 `object_type`

原因：

- 历史实体强行回填 `FACT` 或 `DIM` 风险很高
- 先兼容旧数据，再通过用户逐步补齐更稳妥

### 11.3 回填建议

后续可增加一个“语义补全建议”能力，对历史对象给出推荐值但不自动保存：

- 实体：推荐 `FACT / DIM`
- 属性：推荐 `usage / ref / required_filter`

## 12. 实施顺序

建议按四步实施：

### 第一步：后端模型与 Schema

- 修改 `models.py`
- 修改 `schemas.py`
- 修改 `main.py` 补列逻辑
- 修改 `ontology.py` CRUD 序列化与反序列化

### 第二步：前端建模页

- 修改 `OntologyBuild.vue`
- 修改 `frontend/src/api/index.ts` 的调用参数类型
- 完成实体/属性弹窗和右侧摘要展示

### 第三步：Guide 应用链路

- 修改 `ontology_guide_service.py`
- 扩展预览 payload 与应用落库逻辑

### 第四步：映射联动

- 修改 `mapping.py`
- 让 `ref` 参与 Join 候选排序
- 增加 `FACT / DIM` 的非阻断校验提示

## 13. 验收标准

### 13.1 功能验收

- 用户可以在实体上维护 `object_type`
- 用户可以在属性上维护 `unit / constraint / usage / required_filter / ref`
- 用户可以在实体上维护治理信息
- 列表与详情能正确回显
- Guide 应用不会覆盖或丢失这些字段
- Join 推荐能利用 `ref` 提升命中率

### 13.2 兼容性验收

- 历史数据不因新增字段无法加载
- 未补齐 `object_type` 的历史实体仍可浏览、映射和部署
- 旧的 Guide 预览 payload 仍可应用

## 14. 风险与注意事项

### 14.1 `status` 命名冲突

治理需求里的 `status=active` 与当前实体技术状态 `status=DRAFT/MAPPED/...` 不是一回事，必须使用 `governance_status` 新字段。

### 14.2 `build_type` 认知冲突

前端必须把“构建方式”和“对象类型”分开展示，否则用户会继续把 `TABLE/VIEW` 误解为事实/维度。

### 14.3 `ref` 的真实含义

`ref` 记录的是“业务引用关系”，不是物理数据库外键。它最终会成为 Join 推导依据之一，但不能假设源表一定存在数据库级 FK。

### 14.4 多值字段实现

`usage_codes` 和 `governance_tags` 用 JSON 存储最符合当前项目实现习惯，但查询筛选能力会弱于规范化子表。本期优先交付维护能力，后续若要做复杂检索，再拆表。

## 15. 建议结论

基于当前项目现状，推荐采用“实体表/属性表直接扩列 + 复用现有 CRUD API + 前端弹窗增强 + Guide/映射渐进接入”的实现路线。

这是当前代码基线下改动最小、收益最大、风险可控的方案，且能为后续语义查询、自动 Join 推荐、治理检索和 Agent 分析提示词提供稳定元数据基础。
