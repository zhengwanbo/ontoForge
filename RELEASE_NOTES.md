# ontoForge 功能说明与重大更新记录

**文档版本：2026-08-20**
**适用范围：当前 `main` 分支平台实现与已记录的主要更新**

## 1. 产品概述

ontoForge 是面向企业业务语义建模、知识图谱构建和智能体数据分析的本体管理平台。平台将 Oracle 业务源数据、业务分析域、本体实体/属性/关系、数据映射、Oracle Property Graph 与 Agent Skill 串联，支持从业务需求和关系型表结构出发，逐步形成可部署、可浏览、可查询、可供智能体使用的业务知识图谱。

当前实现重点覆盖制造质量追溯、供应链五码追溯及营销业务分析等场景，也支持通过“业务类型语义管理”扩展到其他业务领域。

## 2. 平台功能说明

### 2.1 业务分析域与业务语义管理

- 创建、维护和删除业务分析域，作为本体、映射、DDL、图谱、流程、规则和 Skill 的业务边界。
- 支持业务类型语义管理，维护业务类型编码、名称、语义说明和语义模式。
- 内置业务主体、制造对象、分析场景、供应链、营销和自定义等语义类型。
- Guide 的 LLM 辅助生成可选择业务语义项目，使生成的对象与关系受该业务语义约束，而非固定套用制造缺陷模板。
- 支持用户—业务分析域授权：管理员拥有全域权限；非管理员只能访问被分配的业务分析域及其数据。

### 2.2 源数据与数据对象管理

- 维护 Oracle 数据源并测试连通性。
- 浏览 Schema、表、字段和有限样例数据。
- 对源表和字段维护或生成业务注释。
- 支持从数据库读取关系表，也支持上传多个 DDL 文件解析表结构。
- 图数据查询可根据当前数据源和已部署的 Property Graph 提供可执行的业务查询建议。

### 2.3 本体关系构建与 Guide 自动生成

- 可手工维护实体、属性、关系及其图形布局；支持属性新增、修改、删除和主键标记。
- 支持上传 `txt`、`md`、`docx`、`pdf` 业务需求文档，提取文本后作为 Guide 输入。
- 支持 LLM 模型辅助生成：先生成最小可行范围设计，再分批生成实体/属性候选，随后生成关系候选和部署建议。
- 对大量关系表实施字段优先级压缩、按表/字段预算分块和模型上下文预算检查，降低大规模表选择导致的上下文超限风险。
- 支持自然语言调整本体设计，并在预览确认后应用为正式实体、属性、关系和初始映射。
- 本体关系图支持关联高亮、动态画布尺寸和纵横滚动，便于维护大规模实体网络。

### 2.4 数据映射与关系实现

- 配置实体、属性和关系到 Oracle 源表/字段的映射。
- 支持全域自动映射、批量应用建议、任务追踪和历史结果回填。
- 支持属性级 `DIRECT`、`COMPUTED`、`CONSTANT`、`LLM_DERIVED` 等映射方式。
- 支持关系 Join 智能识别、Join 物理校验、边 SQL 预览和人工确认。
- 关系实现优先以本体节点表作为 `src` / `dst`，将源表作为 Join 证据与条件识别来源，避免边表直接混用原始业务表。
- 对历史重复边表名、无效 Join、未投影列和未确认映射提供受控处理，减少 DDL 部署失败。

### 2.5 DDL 生成、执行与部署追踪

- 根据已确认的实体、属性和关系映射生成 Oracle 节点、边和 Property Graph DDL。
- 节点和关系优先部署为 Oracle 视图，保留源数据实时性；缺少稳定图键时回退为表。
- 对节点主键、关系 Join、边键、源键和目标键进行部署前校验。
- DDL 执行采用后台异步任务，页面轮询执行状态，避免长时间 Oracle 操作被前端请求超时误判为失败。
- 保存每条 SQL 的执行状态、对象类型、对象名称和错误信息；历史任务支持查看逐条执行明细。
- 失败任务会保留兜底错误明细，防止预检查或后台异常导致历史详情空白。

### 2.6 Oracle Property Graph 浏览与图数据查询

- 从 Oracle `ALL_PG_*` 数据字典读取已部署 Property Graph 的真实顶点、边、标签、键和关系拓扑。
- 优先显示平台本体中的中文实体/关系名称，同时保留 Oracle 技术名称。
- 本体图谱使用 Cytoscape.js 支持有向分层布局、拖拽、缩放、平移、自动布局和详情联动。
- 图实例浏览及 Graph SQL 结果使用 ECharts Graph 展示圆形实例节点、关系箭头、筛选、缩放和上下游链路。
- 支持直接执行受限的 Oracle `GRAPH_TABLE` 查询，并可展示查询结果、关系链路和节点主要属性。
- 针对供应链及营销场景提供常用图查询建议；用户确认后才会执行 SQL。

### 2.7 业务流程、规则和活动

- 支持业务流程定义、节点配置、连线编辑和自动布局。
- 流程画布采用 AntV X6 与 Dagre，支持上下布局、连接桩、缩放、平移、拖拽和长流程适配。
- 支持业务活动、规则目录和规则维护，使业务知识可与本体对象关联。

### 2.8 智能体构建与 Agent Skill

- 支持创建和维护面向业务域、数据源和 Property Graph 的技能配置。
- 可根据 Skill 配置、分析流程和实时图拓扑生成可下载的 Agent Skill ZIP 包。
- 支持上传、查看和删除托管 Skill；上传包校验 `SKILL.md`、文件数量、大小、扩展名和路径安全。
- 托管 Skill 按业务分析域隔离，防止跨域列出、使用或删除 Skill。
- 智能体测试支持多轮对话、结构化问数表格、可审计执行摘要、只读 Graph SQL、执行历史回放和会话持久化。
- Agent 图查询采用“实时图拓扑规划优先、受控模板兜底”机制：模型只能生成受校验的查询计划，后端据此编译只读 SQL，不直接执行模型自由生成的 SQL。

### 2.9 系统管理与安全

- JWT 登录鉴权、用户管理、密码修改、角色兼容和操作日志。
- 管理员可配置用户可访问的业务分析域；非管理员接口侧执行域级授权校验。
- 维护 LLM 配置，支持选择模型、超时、温度和上下文窗口参数。
- 敏感信息应保存在受控环境配置或数据库中；仓库不应提交 `.env`、访问令牌、数据库文件或生产凭据。

## 3. 技术架构

| 层级 | 主要技术 | 职责 |
| --- | --- | --- |
| 前端 | Vue 3、TypeScript、Vite、Element Plus、Pinia、Vue Router | 建模、映射、图谱、流程、智能体和系统管理交互 |
| 图形能力 | Cytoscape.js、ECharts、AntV X6、Dagre、Vue Flow、CodeMirror | 图谱浏览、实例图、流程编辑、SQL 编辑与预览 |
| 后端 | FastAPI、SQLAlchemy、Pydantic、Uvicorn | REST API、领域服务、鉴权、任务编排与数据校验 |
| 数据库 | Oracle Database、python-oracledb、Oracle Property Graph | 平台元数据、源数据、节点/边对象、Property Graph |
| AI 集成 | OpenAI 兼容 Chat Completions | Guide、本体/映射建议、Skill 生成和智能体分析 |

本地开发调用链：

```text
Vue 前端（localhost:5173）
  └─ /api/v1 由 Vite 开发代理转发
       └─ FastAPI（localhost:8000）
            ├─ Oracle 平台元数据与业务源数据
            ├─ Oracle Property Graph
            └─ 配置的 LLM 服务
```

## 4. 重大更新记录

### 2026-08-20 — 域级授权、营销图查询与元数据同步

- 新增用户—业务分析域授权模型和管理界面；管理员可为非管理员分配可用域。
- 在本体、映射、DDL、图谱浏览、图查询、流程、规则和智能体接口加入服务端域级访问控制。
- 托管 Agent Skill 按业务分析域隔离，测试时校验 Skill、数据源和当前域的一致性。
- 图数据查询新增基于真实 Oracle Property Graph 拓扑的营销业务查询建议。
- 新增按 `DOMAIN_ID` 定向同步 Oracle 本体、映射、DDL 与流程元数据的脚本，支持预检、备份、事务回滚与内容摘要校验。

### 2026-08-17 — 异步 DDL 与视图优先部署

- DDL 执行改为后台异步任务，前端可轮询状态并查看逐条 SQL 明细。
- 本体节点与关系边优先生成 Oracle 视图，减少 CTAS 复制并保持源数据实时性。
- 完善节点/边去重、键校验、失败明细兜底和历史任务展示。

### 2026-08-14 — 业务类型语义管理与动态 Guide

- 新增业务类型语义管理，支持维护业务类型、语义说明和模式。
- Guide 的 LLM 辅助模式可选择业务语义项目，支持供应链、营销等非制造缺陷场景。
- 本体关系构建画布支持滚动、关联高亮和属性编辑。

### 2026-08-11 — Agent 图查询规划与多轮会话

- Agent 从“固定模板为主”升级为“实时图拓扑规划优先、受控模板兜底”。
- 支持多跳包装码链路、生产追溯、质检关联、出库至经销商等受控图查询。
- 增加当前问题优先、精确标识过滤、反向路径检索和含糊问题澄清。
- 多轮对话按轮保存表格、解读、SQL 和执行轨迹；测试窗口支持缩放与全屏。

### 2026-08-07 — 图谱可视化、Skill 管理与业务流程画布

- 本体图谱切换为 Cytoscape.js，实例图和查询结果采用 ECharts Graph。
- 新增节点圆形展示、自动布局、实体筛选、节点属性面板和查询实例链路。
- 新增托管 Skill 上传、下载、删除、对话测试、执行过程回放和会话历史。
- 业务流程构建画布升级为 AntV X6 + Dagre。

### 2026-08-06 — Oracle 26ai 供应链与映射/DDL可靠性

- 完成五码追溯 Oracle 26ai DDL、模拟数据、Property Graph 和相关查询验证。
- 强化关系 Join 识别、边表命名、关系映射状态、DDL 部署校验和历史执行明细。
- 新增图实例浏览、上下游全链路展示和本体图中文名称覆盖。

### 2026-07 — Guide、映射与 Property Graph 基础能力

- 建立业务分析域、本体实体/属性/关系、数据映射、DDL 生成、图谱浏览和系统管理基础模块。
- Guide 支持需求文档、DDL、规则数据输入，并引入高层范围文档作为实体和关系生成边界。
- 实现 Oracle Property Graph 直读浏览、图拓扑解析、DDL 部署安全降级和跨 Oracle 元数据同步能力。

## 5. 发布与运维注意事项

- Oracle DDL 会创建、替换或清理节点、边、视图和 Property Graph 对象；应先在开发/测试环境验证。
- Property Graph 浏览需要数据源账号具备 Oracle `ALL_PG_*` 相关元数据读取权限。
- 非管理员用户必须由管理员分配至少一个业务分析域，否则无法访问业务数据。
- LLM 辅助生成结果是可审阅建议；实体、属性、关系映射及 DDL 执行仍应经过业务与技术人员确认。
- Agent 只应执行经过后端约束的只读查询；新增跨表、聚合或高成本图遍历应采用参数化模板、SQL 解析、行数和超时限制。
- 域级元数据同步脚本默认仅预检；实际写入必须显式传入 `--apply` 与备份目录，并应先完成目标库备份。

## 6. 已知限制与后续方向

- 前端生产构建仍有部分大包体积提示，可继续拆分 ECharts、Cytoscape.js 与复杂建模页面的异步模块。
- Guide 和映射模型输出虽经过来源与格式校验，但复杂多表语义仍需要人工确认 Join 条件和部署 SQL。
- DDL 任务目前在任务结束后集中写入语句明细；如需实时逐条进度，可增加执行回调与增量持久化。
- 现有自动化测试中仍有少量历史私有方法和断言预期需要随当前 DDL/Skill 服务接口演进同步更新。
