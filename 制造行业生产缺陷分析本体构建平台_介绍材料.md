# 制造行业生产缺陷分析本体构建平台

## 技术介绍材料

| 项目 | 内容 |
|------|------|
| 文档名称 | 制造行业生产缺陷分析本体构建平台 — 技术介绍材料 |
| 平台底座 | Oracle 26ai 数据库 |
| 文档版本 | V1.0 |
| 编写日期 | 2026-07-28 |

---

## 目录

- [1. 平台概述](#1-平台概述)
- [2. 业务背景与建设目标](#2-业务背景与建设目标)
- [3. 系统架构](#3-系统架构)
- [4. 功能架构](#4-功能架构)
- [5. 功能模块详述](#5-功能模块详述)
- [6. 数据模型设计](#6-数据模型设计)
- [7. 核心技术亮点](#7-核心技术亮点)
- [8. 总结](#8-总结)

---

## 1. 平台概述

### 1.1 平台定位

**制造行业生产缺陷分析本体构建平台**是一个以 Oracle 26ai 数据库为底座，采用本体论（Ontology）方法，结合大语言模型（LLM）与 Agent + Skill 架构，面向制造行业生产缺陷智能分析场景构建的企业级知识工程平台。

平台的核心使命是：**将企业分散在生产系统中的结构化数据，通过本体建模方式转化为具备语义关联的知识图谱**，为后续的缺陷分析、根因追溯、影响评估等智能分析能力提供统一的事实底座。

### 1.2 核心理念

平台围绕三个核心理念展开：

| 理念 | 说明 |
|------|------|
| **本体驱动** | 用本体论方法对企业生产过程进行形式化建模，明确实体、属性、关系，构建可推理的语义网络 |
| **LLM 赋能** | 全流程引入大语言模型辅助：从本体蓝图生成、数据映射推荐、DDL 生成到 Agent 技能构建 |
| **Agent + Skill 协同** | 以"Agent 负责规划调度、Skill 注入领域约束"的分工模式，支撑可演进的智能分析能力 |

### 1.3 关键能力一览

平台已落地的关键能力包括：

- 📊 **源数据浏览与标注** — 可视化浏览 Oracle 源表结构与数据，在线维护 Comments 标注
- 🧩 **业务对象构建** — 图形化定义本体实体、属性、关系，绘制业务分析流程
- 🤖 **LLM 本体蓝图引导** — 上传业务文档/DDL，大模型自动生成本体设计蓝图并一键应用
- 🔗 **数据映射** — 手动映射 + LLM 自动推荐，将本体属性绑定到物理数据源
- 🛠️ **DDL 生成与应用** — 自动生成实体表/视图、关系表、Oracle Property Graph DDL 并执行落地
- 👁️ **本体浏览管理** — 图谱化浏览已构建本体，支持在线增改属性与关系
- ⚡ **智能体技能构建与测试** — 基于本体与流程生成可执行 Skill，支持真实数据测试验证
- ⚙️ **系统管理** — 多数据源管理、大模型配置、用户与日志管理

---

## 2. 业务背景与建设目标

### 2.1 业务背景

在制造行业生产过程中，产品缺陷的识别、根因分析与影响评估是保障产品质量和生产效率的关键环节。传统缺陷分析存在三大痛点：

| 痛点 | 描述 |
|------|------|
| **分析效率低** | 依赖人工经验，跨系统、跨表手工拼接事实，分析周期长 |
| **根因追溯困难** | 缺陷数据分散在缺陷记录、产品信息、工序日志、设备、物料等多张表中，关系不透明，难以追溯 |
| **影响评估不全** | 缺陷与上下游工序、物料的级联关系缺乏统一视图，影响范围评估不完整 |

随着 LLM 与知识图谱技术成熟，基于本体论方法构建企业生产过程知识体系，结合 Agent + Skill 编排，可实现缺陷分析的智能化、自动化与可追溯化。

### 2.2 建设目标

| 目标层级 | 目标描述 |
|----------|----------|
| **总体目标** | 构建面向制造行业的生产缺陷智能分析平台，实现缺陷分析、根因分析、影响分析三大核心能力 |
| **第一阶段目标** | 完成企业生产过程本体对象的构建能力，打通"源数据 → 本体定义 → 数据映射 → DDL 落地 → 本体浏览 → 智能体技能"全流程 |
| **技术目标** | 基于 Oracle 26ai，采用本体论方法，通过 Vue + Python 技术栈，集成 LLM 大模型，实现本体构建全流程的智能化辅助 |

### 2.3 建设范围

第一阶段聚焦于**本体对象构建全流程**，并已扩展至智能体技能构建：

```
源数据管理 → 业务对象构建 → LLM本体蓝图引导 → 数据映射 → DDL生成与应用 → 本体浏览管理 → 智能体技能构建
                                                                    ↓
                                                              系统管理（数据源/大模型/用户/日志）
```

---

## 3. 系统架构

### 3.1 架构总览

系统采用前后端分离的分层架构，整体分为**展示层、应用层、服务层、数据层**四个层次：

```
┌──────────────────────────────────────────────────────────────────────┐
│                          展 示 层 (Vue 3 Frontend)                     │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────┐ ┌────┐ │
│  │源数据    │ │本体构建   │ │数据映射   │ │DDL管理    │ │本体浏览│ │系统│ │
│  │浏览/标注 │ │图形编辑器 │ │映射操作   │ │编辑执行   │ │图谱界面│ │管理│ │
│  └─────────┘ └──────────┘ └──────────┘ └──────────┘ └──────┘ └────┘ │
│                          + Agent技能构建 / 业务规则活动                  │
├──────────────────────────────────────────────────────────────────────┤
│                          应 用 层 (FastAPI Gateway)                    │
│         RESTful API  │  JWT 鉴权  │  文件上传  │  CORS  │  OpenAPI    │
├──────────────────────────────────────────────────────────────────────┤
│                       服 务 层 (Python Backend)                       │
│  ┌─────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐    │
│  │源数据    │ │本体构建   │ │数据映射   │ │DDL生成    │ │本体浏览   │    │
│  │服务      │ │服务      │ │服务      │ │服务      │ │调整服务   │    │
│  └─────────┘ └──────────┘ └──────────┘ └──────────┘ └──────────┘    │
│  ┌───────────────────┐ ┌──────────────────┐ ┌─────────────────────┐  │
│  │ LLM 本体蓝图引导   │ │ Agent 技能服务    │ │ LLM 调用服务         │  │
│  │ (文档/DDL→本体设计) │ │ (技能构建+测试)   │ │ (OpenAI 兼容)        │  │
│  └───────────────────┘ └──────────────────┘ └─────────────────────┘  │
├──────────────────────────────────────────────────────────────────────┤
│                       数 据 层 (Oracle 26ai)                          │
│  ┌───────────┐ ┌───────────┐ ┌───────────┐ ┌─────────────────────┐   │
│  │ 客户源数据  │ │ 平台元数据 │ │ 本体实例   │ │ Oracle Property     │   │
│  │ (业务表)   │ │ (17张SYS表)│ │ (View/Table)│ │ Graph 属性图        │   │
│  └───────────┘ └───────────┘ └───────────┘ └─────────────────────┘   │
└──────────────────────────────────────────────────────────────────────┘
```

### 3.2 技术选型

| 层次 | 技术选型 | 说明 |
|------|----------|------|
| **前端框架** | Vue 3 + TypeScript | Composition API，组件化开发 |
| **前端 UI 库** | Element Plus | 企业级 UI 组件库 |
| **图形编辑器** | Vue Flow (@vue-flow/core) | 本体关系图、业务流程图编辑 |
| **代码编辑器** | CodeMirror 6 + SQL 语法包 | DDL 在线编辑，SQL 语法高亮 |
| **状态管理** | Pinia | Vue 3 官方推荐状态管理 |
| **HTTP 客户端** | Axios | 前后端 API 通信 |
| **后端框架** | Python 3.11+ / FastAPI | 高性能异步框架，自动生成 OpenAPI 文档 |
| **ORM** | SQLAlchemy 2.0 | Oracle 数据库操作 |
| **Oracle 驱动** | oracledb (python-oracledb) | Oracle 官方 Python 驱动 |
| **LLM 集成** | OpenAI 兼容 SDK | 支持 OpenAI / DeepSeek / 通义千问等兼容接口 |
| **数据库** | Oracle 26ai | 利用 Property Graph、JSON、Vector 等多模态能力 |
| **认证** | JWT (JSON Web Token) | 前后端分离认证 |
| **密码安全** | bcrypt 哈希 | 密码安全存储 |

### 3.3 系统分层设计

#### 展示层

- 采用 Vue 3 SPA（单页应用）架构，基于 Vite 构建
- 通过 RESTful API 与后端交互，Axios 封装统一请求拦截
- 图形化操作通过 Vue Flow 实现本体关系图与业务流程图编辑
- DDL 编辑采用 CodeMirror 6，支持 SQL 语法高亮
- JWT Token 认证，路由守卫控制访问权限
- 响应式布局，适配主流桌面浏览器（Chrome、Edge、Firefox）

#### 应用层

- 基于 FastAPI 提供 RESTful API，统一 `/api/v1` 前缀
- JWT 鉴权与基于角色的访问控制（RBAC）
- 请求日志中间件，记录所有 HTTP 请求与响应
- CORS 跨域支持，统一异常处理
- API 文档自动生成（OpenAPI / Swagger）

#### 服务层

- 按业务域划分服务模块，共 6 个核心 Service
- **LLM 本体蓝图引导服务**：解析业务文档/DDL，调用 LLM 生成本体设计
- **Agent 技能服务**：基于本体与流程生成可执行 Skill，支持测试执行
- **DDL 服务**：生成实体表/视图、关系表、Property Graph、语义层 DDL
- **LLM 调用服务**：统一封装大模型交互，支持多模型切换
- 数据库操作通过 SQLAlchemy 统一管理

#### 数据层

- **客户源数据表**：客户提供的 Oracle 业务表（缺陷记录、产品信息、工序日志等）
- **平台元数据表**：17 张 `SYS_*` 表，存储本体定义、映射配置、流程、技能、规则等管理数据
- **本体实例表/视图**：根据 DDL 生成的 `ONTO_*` 前缀物理对象
- **Oracle Property Graph**：基于本体关系构建的属性图，支撑图查询分析

---

## 4. 功能架构

### 4.1 功能模块全景

平台共划分为 **8 大功能模块**，覆盖从源数据到智能体技能的完整构建链路：

```
┌─────────────────────────────────────────────────────────────────────┐
│                  制造行业生产缺陷分析本体构建平台                         │
├───────────────┬───────────────┬───────────────┬─────────────────────┤
│   ① 源数据管理  │  ② 业务对象构建 │ ③ LLM本体蓝图  │   ④ 数据映射         │
│                │               │    引导        │                     │
│ · 数据浏览管理  │ · 业务分析域   │ · 文档解析     │ · 数据映射操作        │
│ · 数据对象标注  │ · 本体关系构建 │ · DDL解析      │ · 数据映射管理        │
│                │ · 业务流程构建 │ · 蓝图生成     │ · LLM自动映射        │
│                │ · 业务规则活动 │ · 蓝图应用     │                     │
├───────────────┼───────────────┼───────────────┼─────────────────────┤
│   ⑤ DDL生成    │  ⑥ 本体浏览    │  ⑦ 智能体构建  │   ⑧ 系统管理         │
│    与应用       │    管理        │               │                     │
│ · DDL自动生成  │ · 本体图谱浏览 │ · 技能构建     │ · 数据源管理          │
│ · 语义层DDL    │ · 节点详情查看 │ · 智能体测试   │ · 大模型管理          │
│ · Property Graph│ · 属性增改    │               │ · 用户管理           │
│ · DDL执行落地  │ · 关系增改     │               │ · 操作日志           │
└───────────────┴───────────────┴───────────────┴─────────────────────┘
```

### 4.2 模块协同关系

各功能模块按"构建链路"顺序协同，形成闭环：

```
        ┌──────────┐
        │ ① 源数据  │ ← 浏览/标注客户业务表
        └────┬─────┘
             │
   ┌─────────┴──────────┐
   ▼                    ▼
┌──────────┐     ┌──────────────┐
│②业务对象  │◄────│③ LLM本体蓝图 │ ← 上传文档/DDL自动生成
│  构建     │     │    引导       │
└────┬─────┘     └──────────────┘
     │
     ▼
┌──────────┐
│④ 数据映射 │ ← 本体属性绑定物理数据源
└────┬─────┘
     │
     ▼
┌──────────┐
│⑤ DDL生成 │ ← 生成表/视图/图并执行落地
└────┬─────┘
     │
     ▼
┌──────────┐         ┌──────────────┐
│⑥ 本体浏览 │────────►│⑦ 智能体技能  │ ← 基于本体+流程构建Skill
│  管理     │         │    构建       │
└──────────┘         └──────────────┘
                            │
                     ⑧ 系统管理 全程支撑
```

### 4.3 功能清单总览

| 模块 | 子功能 | 核心能力 |
|------|--------|----------|
| ① 源数据管理 | 数据浏览管理 | Schema 切换、表结构查看、数据预览查询 |
|  | 数据对象标注 | 表/字段 Comments 在线编辑与持久化 |
| ② 业务对象构建 | 业务分析域管理 | 分析域 CRUD，本体构建的组织容器 |
|  | 本体关系构建 | 图形化实体/属性/关系定义，本体图谱可视化 |
|  | 业务流程构建 | 流程图绘制，节点配置，JSON 持久化 |
|  | 业务规则活动 | 业务规则定义 + 触发活动配置 |
| ③ LLM 本体蓝图引导 | 文档/DDL 解析 | 支持 docx/pdf/txt/md 业务文档与 DDL 脚本解析 |
|  | 蓝图生成 | LLM 分块分析，生成实体/关系/映射/部署设计 |
|  | 蓝图应用 | 一键将蓝图落地为本体元数据 |
| ④ 数据映射 | 数据映射操作 | 实体构建方式选择、属性手动映射、LLM 自动映射 |
|  | 数据映射管理 | 映射状态跟踪、映射后本体图查看、关系映射 |
| ⑤ DDL 生成与应用 | DDL 自动生成 | LLM 生成 + 模板生成，含语义层 DDL |
|  | Property Graph | Oracle 属性图 DDL 生成（顶点表+边表） |
|  | DDL 执行落地 | 在线编辑、执行、日志记录 |
| ⑥ 本体浏览管理 | 本体图谱浏览 | 已构建本体图谱可视化，节点/边详情 |
|  | 在线调整 | 属性增改、关系增改、Comments 维护 |
| ⑦ 智能体构建 | 技能构建 | 基于本体+流程+LLM 生成可执行 Skill 定义 |
|  | 智能体测试 | 真实 graph 表数据 + 流程轨迹 + LLM 执行测试 |
| ⑧ 系统管理 | 数据源管理 | 多 Oracle 数据源配置、测试连接 |
|  | 大模型管理 | LLM 配置 CRUD、测试连接、默认切换 |
|  | 用户管理 | 用户 CRUD、角色（admin/analyst/viewer） |
|  | 操作日志 | 关键操作审计日志 |

---

## 5. 功能模块详述

### 5.1 模块一：源数据管理

#### 5.1.1 功能概述

为用户提供对 Oracle 数据库中源数据表的可视化浏览、查询和标注能力，是本体构建的数据基础。包含两个子功能：**数据浏览管理**和**数据对象标注**。

#### 5.1.2 数据浏览管理

| 功能 | 说明 |
|------|------|
| Schema 切换 | 支持多 Schema 浏览，按 Schema 切换表清单 |
| 源表列表 | 树形/列表展示，支持表名模糊搜索 |
| 表结构查看 | 字段名、数据类型、是否可空、默认值、字段位置 |
| Comments 查看 | 表 Comments + 字段 Comments 一并展示 |
| 数据预览 | 默认前 100 条，表格方式展示 |
| 条件查询 | 支持字段筛选、排序、分页（50/100/200 条） |
| SQL 注入防护 | 参数化查询，禁止字符串拼接 |
| 大字段处理 | CLOB/BLOB 仅显示类型标识 |

#### 5.1.3 数据对象标注

| 功能 | 说明 |
|------|------|
| 表 Comments 编辑 | 在线编辑，执行 `COMMENT ON TABLE` 持久化 |
| 字段 Comments 编辑 | 在线编辑，执行 `COMMENT ON COLUMN` 持久化 |
| 实时刷新 | 修改后立即反映到界面 |
| 操作日志 | 记录操作人、时间、修改前后内容 |

> **设计要点**：源数据只读，平台不修改客户业务表数据（Comments 除外）。Comments 标注的质量直接影响后续 LLM 本体蓝图引导与数据映射的准确性。

---

### 5.2 模块二：业务对象构建

#### 5.2.1 功能概述

提供图形化本体编辑能力，允许业务人员通过可视化方式定义本体实体、属性和关系，绘制数据分析流程图，并配置业务规则与触发活动。按**业务分析域**进行组织管理。

#### 5.2.2 业务分析域管理

业务分析域是本体管理的顶层组织单元，每个分析域下包含独立的本体定义、流程定义、规则定义。

- 域信息：域名称、域类型（BUSINESS）、域描述、状态、创建人
- 分析域之间相互独立，本体定义不共享
- 支持 CRUD，删除时检查关联本体
- 示例：`冲压缺陷分析域`、`焊接缺陷分析域`、`装配缺陷分析域`

#### 5.2.3 本体关系构建

通过 Vue Flow 图形编辑器实现可视化本体建模：

**实体创建：**
- 画布拖拽/点击添加实体节点
- 实体信息：实体名称（同域唯一）、显示名称（中文）、描述、图标/颜色
- 图形位置信息以 JSON 持久化

**属性定义：**
- 属性名称（英文字段名，同实体唯一）、显示名称（中文）
- 数据类型（VARCHAR2/NUMBER/DATE/CLOB 等）
- 是否主键、是否可空、属性描述、排序序号
- 来源标记（PENDING 待映射 / MAPPED 已映射）

**关系定义：**
- 鼠标拖拽拉有向箭头建立关系
- 关系类型：`ONE_TO_ONE`、`ONE_TO_MANY`、`MANY_TO_MANY`、`INHERITANCE`、`ASSOCIATION`
- 关系名称（如：属于、导致、影响、包含、产生于）
- 关系描述、关系表名（多对多时）
- 支持同一对实体间定义多种关系

**本体图谱可视化：**
- 力导向图/层次布局展示完整本体关系图
- 节点显示实体名称和关键属性，边显示关系名称
- 支持缩放、平移、全屏、节点搜索定位

#### 5.2.4 业务流程构建

独立的流程图画布，定义数据分析流程：

**流程节点类型：**

| 节点类型 | 说明 |
|----------|------|
| `start` | 开始节点 |
| `dataInput` | 数据输入节点，引用本体对象作为数据源，配置使用方式（query/aggregate/compare/trace）与时间范围 |
| `analysis` | 分析节点，执行特定分析任务，可关联 Skill |
| `decision` | 决策节点，条件分支 |
| `action` | Action 节点，执行后续操作（generateReport/sendNotification/triggerAlert/updateOntology/customAction） |
| `end` | 结束节点 |

**流程持久化：**
- 画布内容序列化为 JSON，存储到 `SYS_PROCESS_DEF.PROCESS_JSON`
- JSON 含 nodes（节点列表）、edges（连线列表）、version、元数据
- 支持流程版本管理与草稿/发布状态

#### 5.2.5 业务规则活动

将本体对象、关系、属性与业务规则结合，定义规则触发后的可配置活动：

**业务规则（SysBusinessRule）：**
- 规则类别：VALIDATION（校验）/ DECISION（决策）/ DERIVATION（推导）/ ALERT（告警）
- 触发事件：DATA_CREATED / DATA_CHANGED / FLOW_NODE_COMPLETED / MANUAL
- 作用范围：可绑定到实体或关系
- 条件配置：基于属性的阈值、操作符、组合逻辑（JSON）
- 优先级与状态管理

**业务活动（SysBusinessActivity）：**
- 活动类型：NOTIFY（通知）/ CREATE_TASK（创建任务）/ CALL_PROCESS（调用流程）/ DATA_ACTION（数据操作）/ MANUAL_REVIEW（人工审核）
- 可关联分析流程
- 活动参数配置（通知对象、任务参数、接口参数等，JSON）

---

### 5.3 模块三：LLM 本体蓝图引导 ⭐

#### 5.3.1 功能概述

**这是平台的核心亮点功能。** 业务人员只需上传业务文档（需求说明、设计文档）或现有 DDL 脚本，平台调用大语言模型自动分析并生成本体设计蓝图（Blueprint），包含实体、属性、关系、映射建议和部署方案，经人工确认后一键应用到本体元数据。

这一功能将本体构建从"手工逐个定义"升级为"文档驱动自动生成"，大幅降低本体建模门槛。

#### 5.3.2 工作流程

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  上传输入     │───►│  解析预处理   │───►│  LLM 分块分析 │───►│  蓝图合并    │
│              │    │              │    │              │    │              │
│ · 业务文档    │    │ · docx/pdf/  │    │ · Token 预算 │    │ · 实体候选   │
│   (docx/pdf/ │    │   txt/md 解析│    │   分块       │    │   合并       │
│   txt/md)    │    │ · DDL 解析   │    │ · 实体候选   │    │ · 关系候选   │
│ · DDL 脚本   │    │ · 主键推断   │    │   生成       │    │   去重       │
│              │    │ · 表角色推断 │    │ · 关系候选   │    │              │
└──────────────┘    └──────────────┘    │   生成       │    └──────┬───────┘
                                        └──────────────┘           │
                                                                   ▼
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│  蓝图应用     │◄───│  蓝图存储    │◄───│  设计构建    │◄───│  候选过滤    │
│              │    │              │    │              │    │              │
│ · 落地元数据  │    │ · SYS_ONTO   │    │ · 本体设计   │    │ · 按设计文档 │
│   (实体/属性/│    │   LOGY_BLUE  │    │   (实体/关系)│    │   过滤候选   │
│   关系/映射) │    │   PRINT      │    │ · 映射设计   │    │ · 中文关系名│
│ · 生成图位置 │    │ · 版本管理   │    │ · 部署设计   │    │   偏好       │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
```

#### 5.3.3 输入解析能力

**业务文档解析：**
- 支持 docx、pdf、txt、md 格式
- docx：提取段落文本
- pdf：逐页提取文本
- 结构化解析业务文档，提取业务概念描述

**DDL 脚本解析：**
- 解析 `CREATE TABLE` / `CREATE VIEW` 语句
- 提取表名（含 Owner 前缀）、字段名、数据类型
- 解析 `COMMENT ON TABLE` / `COMMENT ON COLUMN`
- 主键推断（基于字段名约定，如 `*_ID`）
- SQL 标识符规范化处理

**表角色推断：**
- 根据表名和字段特征推断源表角色（如主数据表、事务表、配置表等）
- 构建源表语义绑定，辅助 LLM 理解表用途

#### 5.3.4 LLM 分块分析策略

面对大表（字段众多）和大量源表，平台采用智能分块策略控制 Token 预算：

| 策略 | 说明 |
|------|------|
| Token 预算估算 | 预估每块 Token 消耗，动态分块 |
| 表级分块 | 按 Token 预算将多张表打包为一个 chunk |
| 大表二分切片 | 单表字段过多时，按字段二分为多个 segment 分别分析 |
| 列评分筛选 | `_score_column_for_llm` 对列评分，优先保留高价值列送入 LLM |
| 样例数据压缩 | `_compact_sample_rows_for_llm` 压缩样例数据减少 Token |

每个 chunk 调用 LLM 生成实体候选与关系候选，多 chunk 结果通过 `_merge_entity_candidate_results` 合并去重。

#### 5.3.5 蓝图设计构建

合并后的候选经过设计构建，形成完整的本体蓝图包：

**本体设计（_build_ontology_design）：**
- 实体定义（名称、显示名、描述、属性）
- 关系定义（源/目标实体、关系名、类型）
- 中文关系名偏好处理
- 关系名从描述中提取

**映射设计（_build_mapping_design）：**
- 实体到源表的绑定
- 属性到源字段的映射建议
- 构建方式建议（View/Table）

**部署设计（_build_deployment_design）：**
- 生成表名前缀（基于分析域）
- 视图后缀规则（基于源表角色）
- 视图令牌清洗
- 语义层视图命名规范

#### 5.3.6 蓝图存储与应用

**蓝图存储：**
- 蓝图包保存到 `SYS_ONTOLOGY_BLUEPRINT` 表
- 含 `BLUEPRINT_JSON`（完整蓝图）和 `SUMMARY_JSON`（摘要）
- 支持版本管理（GENERATED/APPLIED/ARCHIVED）

**蓝图应用（apply_blueprint）：**
- 将蓝图中的实体、属性、关系落地到 `SYS_ONTOLOGY_ENTITY` / `SYS_ONTOLOGY_PROPERTY` / `SYS_ONTOLOGY_RELATION`
- 将映射配置落地到 `SYS_ENTITY_MAPPING` / `SYS_PROPERTY_MAPPING` / `SYS_RELATION_MAPPING`
- 自动生成图形位置布局
- 应用后本体可在图形编辑器中继续调整

---

### 5.4 模块四：数据映射

#### 5.4.1 功能概述

将本体对象定义与物理数据库表进行映射，完成实体对象的实例化构建过程。明确每个属性的来源表、来源字段、计算方法。支持 LLM 辅助自动映射与人工审核确认。包含**数据映射操作**和**数据映射管理**两个子功能。

#### 5.4.2 实体构建方式

每个本体实体可选择两种构建方式：

| 构建方式 | 说明 | 适用场景 |
|----------|------|----------|
| **Management View** | 创建数据库视图（VIEW） | 实体数据完全来源于源表查询聚合，不需独立存储 |
| **Management Table** | 创建物理表（TABLE） | 实体需独立存储数据，或通过 ETL 填充 |

#### 5.4.3 属性映射类型

| 映射类型 | 说明 |
|----------|------|
| `DIRECT` | 直接映射，属性值取自源表字段 |
| `COMPUTED` | 计算映射，通过公式从多个字段计算 |
| `CONSTANT` | 常量映射，固定值 |
| `LLM_DERIVED` | LLM 推导映射，需人工确认 |

映射信息包含：源表名、源字段名、映射类型、计算公式表达式（SQL）、计算公式描述（自然语言）、置信度（HIGH/MEDIUM/LOW）、映射状态。

#### 5.4.4 LLM 辅助自动映射

系统调用 LLM，输入本体实体定义与源表元数据（表名、Comments、字段名、类型、字段 Comments），LLM 分析语义匹配关系，输出：
- 每个属性推荐的源表和源字段
- 匹配置信度（high/medium/low）
- 匹配理由说明

支持批量映射和单实体映射两种模式。映射结果以对比表格展示，用户逐条或批量审核（接受/修改/拒绝），确认后映射状态更新为"已确认"。

#### 5.4.5 关系映射

为本体关系定义物理层面的关联条件：
- 源实体对应表/视图、目标实体对应表/视图
- 关联条件（JOIN ON 条件）
- 边 SQL（edge_sql，用于图查询边视图）
- 多对多关系需中间关系表

#### 5.4.6 映射状态管理

跟踪每个实体/属性的映射状态流转：
```
PENDING（待映射）→ IN_PROGRESS（映射中）→ CONFIRMED（已确认）/ REJECTED（已拒绝）
```

---

### 5.5 模块五：DDL 生成与应用

#### 5.5.1 功能概述

基于映射完成后的本体定义，生成创建本体对象表/视图的 DDL 语句，以及 Oracle Property Graph 构建的 DDL 语句。用户可检查、修改 DDL，确认后在 Oracle 数据库中执行创建。

#### 5.5.2 DDL 生成能力

平台支持两类 DDL 生成方式：

**① LLM 生成：** 调用大模型，输入本体定义 + 映射配置 + 关系定义，LLM 生成完整 DDL 脚本。

**② 模板生成（语义层 DDL）：** 基于本体蓝图（Blueprint）的语义层设计，通过模板自动生成高质量 DDL，包含：

| DDL 类型 | 生成方法 | 说明 |
|----------|----------|------|
| 实体表 DDL | `_generate_table_ddl` | Management Table 方式，CREATE TABLE + 主键 + 计算列 |
| 实体视图 DDL | `_generate_view_ddl` | Management View 方式，CREATE OR REPLACE VIEW |
| 语义视图 DDL | `_generate_semantic_view_ddl` | 基于蓝图语义层，含字段映射与计算公式 |
| 关系表 DDL | `_generate_relation_table_ddl` | 多对多关系中间表，含外键约束 |
| 边视图 DDL | `_generate_edge_view_ddl` / `_generate_relation_edge_view_ddls` | 图查询边视图，支撑 Property Graph |
| Comments DDL | `_generate_comments_ddl` | COMMENT ON TABLE / COMMENT ON COLUMN |
| Property Graph DDL | `_generate_property_graph_ddl` | Oracle 属性图（顶点表 + 边表） |

#### 5.5.3 Oracle Property Graph DDL

平台自动生成 Oracle 26ai 原生属性图 DDL，将本体实体映射为顶点表（Vertex Tables），本体关系映射为边表（Edge Tables）：

```sql
CREATE PROPERTY GRAPH production_defect_graph
    VERTEX TABLES (
        ONTO_DEFECT_RECORD AS DefectRecord
            KEY (DEFECT_ID)
            PROPERTIES (DEFECT_ID, PRODUCT_ID, DEFECT_TYPE, ...),
        ONTO_PRODUCT_INFO AS Product
            KEY (PRODUCT_ID)
            PROPERTIES (...)
    )
    EDGE TABLES (
        ONTO_REL_DEFECT_TYPE AS CAUSED_BY
            SOURCE KEY (DEFECT_ID) REFERENCES DefectRecord (DEFECT_ID)
            DESTINATION KEY (DEFECT_TYPE_ID) REFERENCES DefectType (DEFECT_TYPE_ID)
            PROPERTIES (RELATION_DESC)
    );
```

构建完成后，可通过 PGQL（Property Graph Query Language）进行图遍历查询，支撑根因追溯与影响分析。

#### 5.5.4 DDL 检查、编辑与执行

- **在线编辑**：CodeMirror 6 代码编辑器，SQL 语法高亮，DDL 按实体分组折叠
- **执行模式**：支持"执行全部"或"逐条执行"
- **冲突处理**：目标对象已存在时，支持跳过/替换（DROP+CREATE）/取消
- **执行日志**：记录执行时间、执行人、DDL 内容、结果（成功/失败）、错误信息、耗时，存入 `SYS_DDL_LOG`

---

### 5.6 模块六：本体浏览管理

#### 5.6.1 功能概述

对已构建到数据库中的本体对象（`ONTO_*` 前缀）进行浏览、查看和在线管理。支持图谱方式浏览、节点详情查看、Comments 修改、属性增改、关系增改。

#### 5.6.2 核心功能

| 功能 | 说明 |
|------|------|
| 本体图谱浏览 | 读取已构建本体对象与关系表，图谱可视化（节点=实体，边=关系） |
| 构建方式区分 | 节点颜色/图标区分 View（蓝色）/ Table（绿色） |
| 节点详情查看 | 表名/视图名、描述（Comments）、构建方式、属性列表（含映射来源） |
| 节点 Comments 修改 | 在线编辑表 Comments，执行 `COMMENT ON TABLE` |
| 属性 Comments 修改 | 在线编辑字段 Comments，执行 `COMMENT ON COLUMN` |
| 属性新增 | 选择源表字段或输入计算公式，生成 `ALTER TABLE ADD COLUMN` 或重建视图 DDL |
| 属性修改 | 修改映射来源或计算方法，生成 `ALTER TABLE MODIFY` 或重建视图 DDL |
| 关系新增 | 图谱拉箭头，定义关系信息与关联条件，生成关系表 DDL 并执行 |
| 关系修改 | 选中关系边，修改条件，生成 ALTER DDL 执行 |
| 节点数据预览 | 预览已构建本体节点（表/视图）的数据 |
| 图谱交互 | 缩放、平移、全屏、导出、按分析域筛选、节点搜索定位 |

> **设计要点**：浏览管理模块的在线调整能力，使本体在 DDL 落地后仍可持续演进，同步更新元数据与物理对象。

---

### 5.7 模块七：智能体构建 ⭐

#### 5.7.1 功能概述

**这是平台面向 AI 智能分析的进阶能力。** 基于已构建的本体对象、业务流程和大模型配置，生成可执行的 Agent 技能（Skill）定义，并支持在真实数据上进行测试验证。

平台采用 **"Agent 负责规划调度、Skill 注入领域约束"** 的协同模式：
- **Agent**：负责意图识别、任务分解、Skill 调度
- **Skill**：封装特定领域分析能力，含分析目标、执行规则、输出要求、Prompt 模板

#### 5.7.2 技能构建（SkillBuilder）

技能定义（SysAgentSkill）绑定以下要素：

| 要素 | 说明 |
|------|------|
| 分析域 | 技能所属业务域 |
| 大模型配置 | 技能执行使用的 LLM |
| 业务流程 | 技能执行所依据的分析流程 |
| 本体对象 | 技能分析的核心实体 |
| 技能名称/描述 | 技能标识与职责说明 |
| 分析目标 | 技能要完成的业务分析目标 |
| 执行规则 | 执行顺序、风险控制、异常处理 |
| 输出要求 | 输出结构与重点 |
| Prompt 模板 | LLM 执行提示词正文 |
| 上下文 JSON | 域/流程/实体/属性/关系的结构化上下文 |

**LLM 辅助技能蓝图生成：**

创建/更新技能时，平台调用 LLM 自动生成技能定义的关键内容：
- 输入：分析域、本体对象（属性+关系）、业务流程步骤、大模型配置
- LLM 输出（严格 JSON）：skill_desc、analysis_goal、execution_rules、output_requirements、prompt_template
- 用户未填写的内容由 LLM 生成补全，已填写的内容保留

**上下文构建：**

`_build_skill_context` 将域、流程（含拓扑排序后的步骤）、实体（含属性与出入关系）组装为结构化上下文 JSON，供技能执行时使用。

#### 5.7.3 智能体测试（AgentTest）

技能构建完成后，可在真实数据上进行测试验证：

**测试流程：**

```
选择技能 + 数据源 + graph表 + 测试问题
            │
            ▼
┌─────────────────────────┐
│ 1. 加载技能上下文         │  ← context_json
├─────────────────────────┤
│ 2. 解析流程步骤(拓扑排序) │  ← _extract_process_steps (BFS)
├─────────────────────────┤
│ 3. 拉取 graph 表详情     │  ← 字段 + 样例数据
├─────────────────────────┤
│ 4. 字段匹配(本体↔物理表) │  ← _match_columns_with_entity
├─────────────────────────┤
│ 5. 构建流程执行轨迹       │  ← _build_process_trace
├─────────────────────────┤
│ 6. 生成建议 SQL          │  ← 基于匹配字段
├─────────────────────────┤
│ 7. 构建 Prompt           │  ← 技能模板 + 上下文
├─────────────────────────┤
│ 8. LLM 执行分析          │  ← call_llm
└─────────────────────────┘
            │
            ▼
   返回结构化测试结果
```

**测试结果包含：**
- 技能信息 + 执行模型信息
- 测试上下文（数据源、表、问题）
- 字段匹配结果（本体属性 ↔ 物理字段）
- 建议 SQL
- Prompt 预览
- Agent 输出（LLM 生成的五段式分析：结论摘要/流程执行说明/关键发现/风险与不确定性/建议动作）
- 流程执行轨迹
- 表预览（字段 + 样例数据）
- 预期输出与建议后续动作

> **设计要点**：智能体测试打通了"本体定义 → 流程编排 → 真实数据 → LLM 执行"的完整链路，让业务人员在技能上线前即可验证分析效果。

---

### 5.8 模块八：系统管理

#### 5.8.1 数据源管理

支持配置多个 Oracle 数据源，实现本体构建与多业务库的灵活对接：

| 配置项 | 说明 |
|--------|------|
| 配置名称 | 如"生产环境Oracle" |
| 数据库类型 | oracle（可扩展 mysql/pg） |
| 主机/端口 | 连接地址 |
| Service Name / SID | Oracle 连接标识 |
| 用户名/密码 | 密码加密存储 |
| Schema | 默认 Schema |
| 业务域绑定 | 数据源可绑定到业务分析域 |
| 启用/默认 | 支持多个数据源，可设默认 |
| 连接状态 | CONNECTED/DISCONNECTED/UNKNOWN |
| 测试连接 | 验证数据源可用性 |

#### 5.8.2 大模型管理

管理 OpenAI 兼容大模型的连接配置：

| 配置项 | 说明 |
|--------|------|
| 配置名称 | 如"生产环境-GPT4" |
| API Base URL | 大模型服务地址 |
| API Key | 加密存储，界面脱敏 |
| 模型名称 | gpt-4o / deepseek-chat 等 |
| 温度参数 | Temperature |
| 最大 Token | 单次请求最大 Token |
| 上下文窗口 | context_window_tokens |
| 请求超时 | 超时时间（秒） |
| 启用/默认 | 支持多配置并存，可设默认 |
| 测试连接 | 发送测试消息验证可用性 |

支持 OpenAI、Azure OpenAI、DeepSeek、通义千问等兼容接口的大模型服务。

#### 5.8.3 用户管理

| 功能 | 说明 |
|------|------|
| 用户 CRUD | 创建、编辑、删除用户 |
| 角色体系 | admin（系统管理员）/ analyst（业务分析师）/ viewer（只读） |
| 启用/禁用 | 用户状态管理 |
| 密码修改 | 当前密码 + 新密码 + 确认，bcrypt 哈希存储 |
| 密码强度 | 至少 8 位，包含字母和数字 |

#### 5.8.4 操作日志

记录关键操作审计日志（`SYS_OPERATION_LOG`）：
- 操作人、操作类型、操作目标
- 操作详情、修改前值、修改后值
- 操作时间

---

## 6. 数据模型设计

### 6.1 数据库总体结构

```
Oracle 26ai 数据库
├── 客户源数据 Schema（客户提供）
│   ├── DEFECT_RECORDS          -- 缺陷记录表
│   ├── PRODUCT_INFO            -- 产品信息表
│   ├── PROCESS_LOG             -- 工序日志表
│   └── ...                     -- 其他业务表
│
├── 平台管理 Schema（平台自建，17 张 SYS_* 表）
│   ├── SYS_DOMAIN              -- 业务分析域
│   ├── SYS_ONTOLOGY_ENTITY     -- 本体实体定义
│   ├── SYS_ONTOLOGY_PROPERTY   -- 本体属性定义
│   ├── SYS_ONTOLOGY_RELATION   -- 本体关系定义
│   ├── SYS_ENTITY_MAPPING      -- 实体数据映射配置
│   ├── SYS_PROPERTY_MAPPING    -- 属性数据映射配置
│   ├── SYS_RELATION_MAPPING    -- 关系映射配置
│   ├── SYS_PROCESS_DEF         -- 分析流程定义（JSON）
│   ├── SYS_AGENT_SKILL         -- Agent 技能定义
│   ├── SYS_BUSINESS_RULE       -- 业务规则
│   ├── SYS_BUSINESS_ACTIVITY   -- 业务活动
│   ├── SYS_ONTOLOGY_BLUEPRINT  -- 本体蓝图（LLM 生成）
│   ├── SYS_LLM_CONFIG          -- 大模型配置
│   ├── SYS_DATA_SOURCE         -- 数据源配置
│   ├── SYS_MAPPING_TASK        -- 映射任务
│   ├── SYS_DDL_LOG             -- DDL 执行日志
│   ├── SYS_USER                -- 用户信息
│   └── SYS_OPERATION_LOG       -- 操作日志
│
├── 本体实例 Schema（DDL 生成）
│   ├── ONTO_DEFECT_RECORD      -- 缺陷记录（表或视图）
│   ├── ONTO_PRODUCT_INFO       -- 产品信息
│   ├── ONTO_REL_*              -- 关系表/边视图
│   └── ...
│
└── Oracle Property Graph
    └── PRODUCTION_DEFECT_GRAPH -- 生产缺陷属性图
```

### 6.2 核心元数据表

#### 本体定义类

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| SYS_DOMAIN | 业务分析域 | domain_id, domain_name, domain_type, status |
| SYS_ONTOLOGY_ENTITY | 本体实体 | entity_id, domain_id, entity_name, build_type(VIEW/TABLE), table_name, status(DRAFT/MAPPED/DDL_GENERATED/DEPLOYED), graph_position |
| SYS_ONTOLOGY_PROPERTY | 本体属性 | property_id, entity_id, property_name, data_type, is_primary_key, source_mark(PENDING/MAPPED) |
| SYS_ONTOLOGY_RELATION | 本体关系 | relation_id, domain_id, source_entity_id, target_entity_id, relation_name, relation_type, relation_table_name |

#### 映射配置类

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| SYS_ENTITY_MAPPING | 实体映射 | entity_id, build_type, view_sql, mapping_status |
| SYS_PROPERTY_MAPPING | 属性映射 | property_id, source_table, source_column, mapping_type(DIRECT/COMPUTED/CONSTANT/LLM_DERIVED), formula_expr, confidence |
| SYS_RELATION_MAPPING | 关系映射 | relation_id, source_table, target_table, join_condition, edge_sql |

#### 流程与智能体类

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| SYS_PROCESS_DEF | 分析流程 | domain_id, process_name, process_json(CLOB), status(DRAFT/PUBLISHED) |
| SYS_AGENT_SKILL | Agent 技能 | domain_id, llm_config_id, process_id, entity_id, analysis_goal, execution_rules, output_requirements, prompt_template, context_json |
| SYS_BUSINESS_RULE | 业务规则 | domain_id, rule_category, trigger_event, condition_json, activity_id, priority |
| SYS_BUSINESS_ACTIVITY | 业务活动 | domain_id, activity_type(NOTIFY/CREATE_TASK/CALL_PROCESS/DATA_ACTION/MANUAL_REVIEW), config_json |

#### 蓝图与系统类

| 表名 | 说明 | 关键字段 |
|------|------|----------|
| SYS_ONTOLOGY_BLUEPRINT | 本体蓝图 | domain_id, version_no, status(GENERATED/APPLIED/ARCHIVED), blueprint_json, summary_json |
| SYS_LLM_CONFIG | 大模型配置 | config_name, api_base_url, api_key_enc, model_name, temperature, context_window_tokens |
| SYS_DATA_SOURCE | 数据源 | source_name, host, port, service_name, username, password_enc, business_domain_id |
| SYS_MAPPING_TASK | 映射任务 | task_type(BULK_GENERATE/BULK_APPLY/ENTITY_RERUN), request_json, result_json, summary_json |
| SYS_DDL_LOG | DDL 日志 | ddl_content, execution_result, error_message, execution_duration |
| SYS_USER | 用户 | username, password_hash, role(admin/analyst/viewer), status |
| SYS_OPERATION_LOG | 操作日志 | operator, operation_type, before_value, after_value |

### 6.3 实体状态流转

本体实体（SYS_ONTOLOGY_ENTITY.STATUS）的状态流转：

```
DRAFT ──本体定义完成──► (映射) ──► MAPPED ──DDL生成──► DDL_GENERATED ──DDL执行──► DEPLOYED
```

---

## 7. 核心技术亮点

### 7.1 LLM 本体蓝图引导：文档驱动建模 ⭐

**传统痛点**：本体建模需要领域专家 + 本体工程师协作，逐个定义实体、属性、关系，门槛高、耗时长。

**平台方案**：业务人员上传业务文档或现有 DDL，大模型自动分析生成本体设计蓝图。

**技术实现亮点：**
- **多格式文档解析**：docx/pdf/txt/md 自动提取文本
- **DDL 智能解析**：解析 CREATE 语句、Comments、主键推断
- **Token 预算分块**：大表二分切片 + 列评分筛选 + 样例数据压缩，确保 LLM 输入可控
- **多 chunk 结果合并**：实体候选去重、关系候选合并
- **设计文档过滤**：候选按业务文档语义过滤，提升准确性
- **一键应用**：蓝图落地为元数据，无需手工重建

### 7.2 语义层 DDL：蓝图驱动的工程化生成 ⭐

**平台方案**：基于本体蓝图的语义层设计，通过模板自动生成高质量 DDL，而非完全依赖 LLM 自由生成。

**生成的 DDL 类型：**
- 实体表/视图 DDL（含计算列、主键、Comments）
- 关系表 DDL（含外键约束）
- **边视图 DDL**（Edge View，支撑图查询）
- **Oracle Property Graph DDL**（顶点表 + 边表映射）
- Comments DDL

**优势**：模板生成保证 DDL 规范性与一致性，LLM 生成作为补充，两者可合并使用。

### 7.3 Agent 技能构建与测试闭环 ⭐

**平台方案**：将"本体 + 流程 + 大模型"三者绑定，生成可执行技能定义，并在真实数据上测试。

**技术实现亮点：**
- **LLM 辅助技能蓝图**：自动生成分析目标、执行规则、输出要求、Prompt 模板
- **流程拓扑排序**：BFS 算法对流程节点拓扑排序，生成有序执行步骤
- **字段智能匹配**：本体属性与物理表字段自动匹配（名称相似度）
- **流程执行轨迹**：为每个流程节点生成具体执行动作描述
- **建议 SQL 生成**：基于匹配字段自动生成查询 SQL
- **LLM 五段式执行**：结论摘要/流程执行说明/关键发现/风险与不确定性/建议动作

### 7.4 多数据源与多模型灵活配置

- **多数据源**：支持配置多个 Oracle 数据源，绑定到不同业务域，实现跨库本体构建
- **多模型**：支持 OpenAI/DeepSeek/通义千问等兼容接口，不同技能可使用不同模型
- **模型规范化**：`normalize_model_name` 自动处理模型名称，适配不同 provider

### 7.5 业务规则与活动引擎

将本体与业务规则结合，实现数据驱动的自动化：
- 基于本体对象、关系、属性定义业务规则
- 规则触发事件（数据变更、流程节点完成、手动）
- 触发可配置活动（通知、创建任务、调用流程、数据操作、人工审核）
- 为后续 Agent 自动化执行奠定基础

### 7.6 全流程 LLM 赋能

LLM 在平台各环节发挥辅助作用：

| 环节 | LLM 作用 |
|------|----------|
| 源数据标注 | （预留）辅助 Comments 推荐 |
| 本体蓝图引导 | 文档/DDL → 本体设计 |
| 数据映射 | 属性 ↔ 源字段自动推荐 |
| DDL 生成 | 本体定义 → DDL 脚本 |
| 技能构建 | 本体+流程 → 技能定义 |
| 技能测试 | 真实数据 → 分析结论 |

---

## 8. 总结

### 8.1 平台价值

制造行业生产缺陷分析本体构建平台以 Oracle 26ai 为底座，通过本体论方法 + LLM 赋能 + Agent + Skill 架构，实现了从源数据到智能体技能的完整知识工程闭环：

| 价值维度 | 体现 |
|----------|------|
| **降低建模门槛** | LLM 本体蓝图引导，业务人员上传文档即可生成本体设计 |
| **提升构建效率** | 全流程 LLM 辅助，从手工建模到文档驱动自动生成 |
| **保证工程质量** | 语义层模板 DDL + Property Graph 标准化生成 |
| **支撑智能分析** | Agent 技能构建与测试闭环，为缺陷分析/根因追溯/影响评估奠定基础 |
| **可持续演进** | 本体浏览管理支持在线调整，技能可迭代优化 |

### 8.2 架构特点

- **四层分离架构**：展示层 / 应用层 / 服务层 / 数据层清晰分离
- **前后端分离**：Vue 3 + FastAPI，技术栈现代化
- **元数据驱动**：17 张 SYS_* 表承载全部平台管理数据，本体实例与元数据分离
- **图谱原生**：利用 Oracle 26ai Property Graph 原生能力，无需额外图数据库
- **LLM 无关**：OpenAI 兼容接口，支持多模型灵活切换

### 8.3 已具备能力

平台第一阶段已完整落地以下能力：

✅ 源数据浏览与标注
✅ 业务对象构建（分析域 + 本体关系 + 业务流程 + 业务规则活动）
✅ LLM 本体蓝图引导（文档/DDL → 本体设计 → 一键应用）
✅ 数据映射（手动 + LLM 自动 + 审核确认）
✅ DDL 生成与应用（实体/关系/语义层/Property Graph + 执行落地）
✅ 本体浏览管理（图谱浏览 + 在线调整）
✅ 智能体技能构建与测试
✅ 系统管理（多数据源 + 多模型 + 用户 + 日志）

### 8.4 演进方向

后续阶段可基于已建成的本体底座与技能框架，继续扩展：

- 缺陷分析 Agent 智能体实现（完整 Agent 编排引擎）
- 缺陷根因分析 Skill 开发（基于 Oracle Graph 图遍历）
- 缺陷影响分析 Skill 开发（关系级联遍历）
- 数据分析流程执行引擎（流程定义 JSON 解析执行）
- 分析后 Action 自动化执行（业务活动引擎落地）
- Vector Embedding 语义搜索（缺陷描述向量化）

---

*文档结束*
